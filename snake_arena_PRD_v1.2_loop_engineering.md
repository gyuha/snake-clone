# SERPENT ARENA

**온라인 멀티플레이 스네이크 아레나 게임 PRD**

Snake.io 장르 문법을 참고한 독자 구현 PRD 및 AI 에이전트 기반 개발 운영 가이드

**통합판 v1.2 · Product Requirements + Loop Engineering Playbook**

| **문서 버전**   | **1.2**                                                                        |
|-----------------|--------------------------------------------------------------------------------|
| **작성일**      | 2026-07-13                                                                     |
| **제품명**      | Serpent Arena (가칭)                                                           |
| **대상 플랫폼** | Desktop/Mobile Web, PWA                                                        |
| **권장 스택**   | React + Phaser + TypeScript + Colyseus + PostgreSQL + Redis                    |
| **문서 상태**   | 개발 착수용 통합 기준안 · 대형 맵 네트워크와 Loop Engineering 반영             |
| **주요 개정**   | Room/AOI, 예측·보간, 경로·델타 압축, 공간 해시, 작업 루프·검증기·안전장치 추가 |

**IMPORTANT**

본 문서는 기존 서비스의 소스·상표·캐릭터·그래픽·음원·UI를 복제하지 않고, 공개적으로 관찰 가능한 장르 메커니즘을 바탕으로 별도의 제품을 구현하기 위한 문서입니다.

# 문서 사용 안내

> **개발 기준**
>
> MVP는 “웹에서 즉시 시작되는 5분 내외 실시간 아레나 대전”에 집중합니다. 계정, 스킨, 이벤트, 친구 기능은 코어 플레이가 검증된 뒤 확장합니다.

본 PRD는 기획, 게임 규칙, UI/UX, 서버 아키텍처, API/프로토콜, 데이터 모델, 인프라, 운영, 테스트, 일정과 승인 기준을 하나의 개발 기준선으로 묶습니다. 값이 “초기값”으로 표시된 항목은 밸런스 테스트를 통해 운영 설정으로 조정할 수 있어야 합니다.

## 목차

| **번호** | **장**                                              |
|----------|-----------------------------------------------------|
| **1**    | 제품 개요와 목표                                    |
| **2**    | 레퍼런스 분석과 독자 구현 원칙                      |
| **3**    | 사용자와 핵심 시나리오                              |
| **4**    | 범위와 출시 단계                                    |
| **5**    | 게임 디자인 요구사항                                |
| **6**    | 기능 요구사항                                       |
| **7**    | 화면·UX 요구사항                                    |
| **8**    | 기술 아키텍처                                       |
| **9**    | 게임 서버 상세 설계                                 |
| **10**   | 프런트엔드 상세 설계                                |
| **11**   | REST API와 WebSocket 프로토콜                       |
| **12**   | 데이터 모델                                         |
| **13**   | 인프라·배포·운영                                    |
| **14**   | 보안·부정행위 대응                                  |
| **15**   | 비기능 요구사항                                     |
| **16**   | 분석·KPI·라이브 운영                                |
| **17**   | 테스트 전략                                         |
| **18**   | 개발 로드맵                                         |
| **19**   | 리스크와 의사결정                                   |
| **20**   | 출시 승인 기준                                      |
| 21       | Loop Engineering 기반 구현 운영 가이드              |
| 부록     | 모노레포·설정값·루프 템플릿·검증 스크립트·참고 자료 |

# 1. 제품 개요와 목표
## 1.1 한 문장 제품 정의

> **제품 정의**
>
> 브라우저에서 설치 없이 접속해, 짧은 시간 동안 다른 플레이어와 성장·회피·유도 충돌을 겨루는 캐주얼 실시간 멀티플레이 2D 아레나 게임.

## 1.2 문제 정의
- 복잡한 튜토리얼이나 장시간 플레이 없이도 즉시 경쟁의 재미를 느낄 수 있는 웹 게임이 필요하다.

- 모바일과 데스크톱에서 동일한 계정·진행도·조작 감각을 제공해야 한다.

- 실시간 멀티플레이 특유의 지연과 부정행위를 통제하면서도 1인 또는 소규모 팀이 운영 가능한 구조가 필요하다.

- 코어 플레이가 검증된 후 스킨, 미션, 시즌 이벤트를 추가할 수 있는 라이브 서비스 기반이 필요하다.

## 1.3 제품 목표

| **ID**   | **목표**                                                         | **성공 기준**                         |
|----------|------------------------------------------------------------------|---------------------------------------|
| **G-01** | 첫 방문 사용자가 10초 안에 게스트로 게임을 시작한다.             | Play 클릭→매치 입장 중앙값 ≤ 10초     |
| **G-02** | 평균 3~6분의 반복 가능한 세션을 만든다.                          | 평균 세션 길이 180~360초              |
| **G-03** | 일반적인 한국·일본 지역 네트워크에서 부드러운 조작감을 제공한다. | P95 입력 체감 지연 ≤ 180ms            |
| **G-04** | 서버가 이동·충돌·점수를 최종 판정한다.                           | 클라이언트 변조로 점수/위치 조작 불가 |
| **G-05** | MVP 이후 콘텐츠를 코드 배포 없이 운영한다.                       | 스킨·미션·이벤트의 설정 기반 노출     |

## 1.4 비목표
- 출시 1차에 원작과 동일한 캐릭터, 스킨, 음원, 맵, 이름, 로고 또는 화면 구성을 재현하지 않는다.

- MVP에서 길드, 실시간 음성 채팅, 복잡한 아이템 경제, NFT/블록체인, 사용자 제작 콘텐츠를 제공하지 않는다.

- 초기에는 100명 이상 대규모 배틀로얄이나 완전한 e스포츠 랭크 시스템을 목표로 하지 않는다.

- 모바일 네이티브 앱은 웹/PWA 지표가 검증된 뒤 Capacitor 또는 별도 클라이언트로 검토한다.

# 2. 레퍼런스 분석과 독자 구현 원칙
## 2.1 공개 기능 관찰

| **영역**        | **관찰 내용**                                                        | **적용 단계** | **근거** |
|-----------------|----------------------------------------------------------------------|---------------|----------|
| **즉시 플레이** | 웹에서 Play를 눌러 게임을 실행하고, 가로 모드를 요구한다.            | MVP 필수      | R1       |
| **조작**        | 마우스, WASD/방향키로 방향 변경; 클릭 또는 Space로 부스트.           | MVP 필수      | R1       |
| **핵심 규칙**   | 먹이를 먹어 성장하고, 상대의 머리가 자신의 몸에 충돌하도록 유도한다. | MVP 필수      | R1       |
| **경쟁**        | 실시간 멀티플레이와 리더보드 경쟁을 강조한다.                        | MVP 필수      | R1       |
| **수집**        | 미션으로 다양한 스킨을 해금한다.                                     | Phase 2       | R2       |
| **라이브 운영** | 월간 테마 이벤트와 별도 모드를 운영한다.                             | Phase 3       | R2/R3    |
| **보스/특수전** | 다른 뱀 및 보스와 싸우는 이벤트 콘텐츠가 소개된다.                   | Phase 3       | R3       |

*관찰 결과는 제품 방향을 정하기 위한 참고 자료일 뿐이며, 실제 수치·알고리즘·자산은 본 문서의 독자 설계를 따른다.*

## 2.2 독자 구현 원칙

| **원칙**        | **요구사항**                                                                                          |
|-----------------|-------------------------------------------------------------------------------------------------------|
| **브랜드 분리** | 제품명, 도메인, 로고, UI 스타일, 캐릭터와 카피를 새로 제작한다.                                       |
| **자산 분리**   | 이미지, 애니메이션, 음원, 폰트, 스킨 디자인을 직접 제작하거나 적법한 라이선스로 구매한다.             |
| **코드 분리**   | 난독화된 웹 파일 분석, 역컴파일, 패킷 복제 없이 공개 문서와 일반적인 게임 네트워크 기법으로 구현한다. |
| **차별화**      | 독자적인 아레나 테마, 위험 지대, 능력 없는 공정한 코스메틱, 시즌 규칙을 설계한다.                     |
| **명확한 표기** | 외부 공개 시 “Snake.io 공식/제휴/후속작”으로 오인될 표현을 사용하지 않는다.                           |

# 3. 사용자와 핵심 시나리오
## 3.1 주요 사용자

| **페르소나**         | **목표**                                                  | **핵심 니즈**                     |
|----------------------|-----------------------------------------------------------|-----------------------------------|
| **P1 캐주얼 방문자** | 로그인 없이 2~5분 즐기고 싶다.                            | 즉시 시작, 직관 조작, 빠른 재도전 |
| **P2 경쟁 사용자**   | 리더보드와 개인 최고 기록을 갱신하고 싶다.                | 공정한 판정, 랭킹, 전적           |
| **P3 수집 사용자**   | 미션과 이벤트로 스킨을 모으고 싶다.                       | 진행도, 보상, 시즌 콘텐츠         |
| **P4 운영자**        | 코드 배포 없이 이벤트·밸런스·공지·금칙어를 조정하고 싶다. | 관리 콘솔, 설정 버전, 감사 로그   |

## 3.2 핵심 사용자 여정

| **단계**      | **사용자 행동**            | **시스템 반응**                                    |
|---------------|----------------------------|----------------------------------------------------|
| **1. 유입**   | 랜딩 페이지 접속           | 게임 소개와 Play CTA가 즉시 보인다.                |
| **2. 준비**   | 닉네임·스킨·지역 자동 선택 | 게스트 세션은 로컬 저장되고 금칙어가 검증된다.     |
| **3. 매칭**   | Quick Play 선택            | 가장 가까운 지역의 수용 가능한 방으로 연결된다.    |
| **4. 플레이** | 이동·먹이·부스트·충돌      | 화면은 부드럽게 렌더링되고 서버가 결과를 판정한다. |
| **5. 결과**   | 순위·점수·생존 시간 확인   | 개인 최고 기록, 보상, 다시하기가 표시된다.         |
| **6. 전환**   | 계정 연결 또는 재도전      | 게스트 진행도를 이메일/소셜 계정에 연결할 수 있다. |

## 3.3 대표 사용자 스토리

| **ID**    | **스토리**                                                      | **인수 조건**                                    |
|-----------|-----------------------------------------------------------------|--------------------------------------------------|
| **US-01** | 방문자로서 회원가입 없이 게임을 시작하고 싶다.                  | 첫 접속→Play→방 입장까지 10초 이내               |
| **US-02** | 플레이어로서 마우스와 키보드 중 편한 조작을 쓰고 싶다.          | 두 입력 체계가 동시에 지원되고 충돌하지 않음     |
| **US-03** | 플레이어로서 네트워크 지연이 있어도 내 뱀이 끊기지 않길 원한다. | 로컬 예측과 서버 보정이 급격한 순간이동을 최소화 |
| **US-04** | 경쟁 사용자로서 내 순위가 실시간으로 바뀌는 것을 보고 싶다.     | 상위 10명과 내 순위가 1초 이내 갱신              |
| **US-05** | 사용자로서 다시하기를 눌러 즉시 재매칭하고 싶다.                | 결과 화면에서 1회 클릭으로 재입장                |
| **US-06** | 운영자로서 특정 스킨과 이벤트를 시작·종료하고 싶다.             | 예약 시간과 대상 세그먼트로 설정 가능            |

# 4. 범위와 출시 단계
## 4.1 MVP 범위

| **에픽**            | **포함 내용**                                       |
|---------------------|-----------------------------------------------------|
| **웹 랜딩/게임 셸** | Play, 닉네임, 설정, 도움말, 반응형 가로 모드        |
| **게스트 인증**     | 익명 ID, 토큰 재발급, 로컬 진행도                   |
| **Quick Match**     | 지역 자동 선택, 빈 방 탐색/생성, 재연결             |
| **실시간 아레나**   | 이동, 부스트, 먹이, 성장, 충돌, 사망, 리스폰/재도전 |
| **리더보드**        | 방 내 실시간 Top 10, 개인 순위                      |
| **기본 스킨**       | 색상/패턴 8~12종, 무료 선택                         |
| **전적**            | 최고 점수, 최고 길이, 생존 시간, 경기 수            |
| **운영 기반**       | 서버 설정, 공지, 금칙어, 방/서버 상태 대시보드      |
| **관측성**          | 로그, 메트릭, 오류 추적, 매치 품질 지표             |

## 4.2 단계별 확장

| **단계**                | **주요 기능**                                                      | **검증 목표**                  |
|-------------------------|--------------------------------------------------------------------|--------------------------------|
| **Phase 1 — MVP**       | Quick Play, 게스트, 1개 기본 모드, 방 리더보드, 기본 스킨          | 코어 재미·네트워크 안정성 검증 |
| **Phase 2 — Retention** | 계정 연결, 일일/주간 미션, 스킨 해금, 글로벌/주간 랭킹, 친구 초대  | 재방문과 수집 동기             |
| **Phase 3 — Live Ops**  | 시즌, 테마 이벤트, 보스/특수 규칙, 배틀패스형 무료 트랙, 운영 콘솔 | 콘텐츠 운영 효율               |
| **Phase 4 — Scale**     | 다지역, 랭크 큐, 파티, 모바일 네이티브 래핑, 고급 부정행위 탐지    | 규모와 수익화                  |

## 4.3 MoSCoW 우선순위

| **우선순위**    | **항목**                                                                      |
|-----------------|-------------------------------------------------------------------------------|
| **Must**        | 권위형 이동/충돌, 빠른 매칭, 리더보드, 재도전, 모바일/데스크톱 입력, 모니터링 |
| **Should**      | 서버 봇, 재연결, 계정 연결, 기본 전적, 관리자 설정                            |
| **Could**       | 미션, 스킨 해금, 친구 초대, 관전자, 시즌 이벤트                               |
| **Won't (MVP)** | 채팅, 길드, 거래, 사용자 생성 스킨, 토너먼트, 음성 통신                       |

# 5. 게임 디자인 요구사항
## 5.1 코어 게임 루프
1.  작은 개체로 스폰한다.

2.  먹이를 수집해 길이와 점수를 키운다.

3.  부스트로 위험을 회피하거나 상대를 압박하되 질량을 소비한다.

4.  상대 머리를 자신의 몸에 충돌시키고 사망 잔해를 수집한다.

5.  더 높은 순위에 도전하다 죽으면 결과를 확인하고 즉시 재도전한다.

## 5.2 초기 밸런스 기준값

> **설정 기반 원칙**
>
> 아래 값은 개발 초기값이며 코드 상수가 아니라 버전 관리되는 GameConfig로 배포해야 합니다. A/B 또는 운영 튜닝 시 게임 서버와 클라이언트가 같은 configVersion을 사용합니다.

| **설정**              | **초기값**                           | **비고**                         |
|-----------------------|--------------------------------------|----------------------------------|
| **Arena size**        | 6,000 × 6,000 world units            | 동시 60명 기준 시작값            |
| **Room capacity**     | 60 players + 최대 20 bots            | 실사용 부하 테스트 후 조정       |
| **Server tick**       | 20 Hz                                | 50ms 고정 스텝                   |
| **Snapshot rate**     | 10 Hz                                | 중요 이벤트는 즉시 메시지        |
| **Client input rate** | 20~30 Hz                             | 변경 입력 우선, 빈도 제한        |
| **Base speed**        | 180 units/s                          | 길이에 따라 최대 10% 보정 가능   |
| **Boost speed**       | 270 units/s                          | 기본 속도의 1.5배                |
| **Boost cost**        | 초당 현재 질량의 0.8% 또는 고정 질량 | 최소 길이 아래 사용 제한         |
| **Spawn protection**  | 1.5초                                | 공격 판정과 부스트 제한          |
| **Match duration**    | 무제한/서버 회전 30분                | 플레이어는 사망 시 세션 종료     |
| **Leaderboard**       | Top 10 + 본인                        | 점수 우선, 생존시간 타이브레이크 |

## 5.3 이동 모델
- 플레이어 입력은 목표 방향 벡터로 표현한다. 클라이언트가 좌표를 직접 전송하지 않는다.

- 서버는 최대 회전 속도, 속도, 길이, 부스트 상태를 적용하여 머리의 다음 위치를 계산한다.

- 몸통은 머리의 이동 경로를 일정 간격으로 샘플링한 세그먼트 체인으로 표현한다.

- 길이가 길수록 시각적 몸통 세그먼트 수가 증가하되, 네트워크 전송은 경로 키포인트로 압축한다.

- 자기 몸통 충돌은 MVP에서 비활성화하고, 경계 충돌은 즉사 또는 소프트 월 중 하나를 설정으로 선택한다.

## 5.4 성장과 점수

| **요소**      | **효과**                              | **설계 의도**                                |
|---------------|---------------------------------------|----------------------------------------------|
| **일반 먹이** | 소량 질량 + 기본 점수                 | 맵 전체에 밀도 기반 스폰                     |
| **사망 잔해** | 상대 질량 일부가 다수 펠릿으로 변환   | 킬 보상보다 위험 지역 수집을 유도            |
| **시간 점수** | 생존 시간에 따라 완만하게 증가        | 잠수 플레이가 우세하지 않게 상한/가중치 적용 |
| **킬 기여**   | 상대가 자신의 몸에 충돌했을 때 보너스 | 마지막 3초 접촉/유도 규칙으로 판정           |
| **콤보**      | 짧은 시간 내 연속 킬 또는 대량 섭취   | Phase 2 이후 검토                            |

## 5.5 충돌과 사망 판정

| **상황**                | **판정**                                                                |
|-------------------------|-------------------------------------------------------------------------|
| **Head vs other body**  | 머리 원과 상대 몸통 캡슐이 교차하면 머리 소유자 사망                    |
| **Head vs head**        | 동일 틱 충돌 시 양쪽 사망을 기본값으로 하며, 핑 차이에 의한 우위를 방지 |
| **Head vs boundary**    | 기본 모드에서는 즉사; 초보 모드는 밀어내기 옵션 가능                    |
| **Spawn overlap**       | 안전 영역 탐색 후 스폰, 1.5초 보호                                      |
| **Disconnected player** | 3초간 마지막 입력 유지 후 감속; 10초 내 재연결 실패 시 사망 처리        |
| **Death conversion**    | 현재 질량의 설정 비율을 펠릿으로 전환하고 나머지는 제거                 |

## 5.6 스폰과 맵 유지
- 플레이어 및 펠릿 밀도를 Uniform Grid 셀 단위로 관리한다.

- 새 플레이어는 다른 머리·몸통·경계에서 최소 안전 거리 이상 떨어진 후보 지점에 스폰한다.

- 먹이는 목표 총량과 셀별 최소 밀도를 기준으로 보충하며, 서버 틱마다 전량 생성하지 않고 예산을 나눠 처리한다.

- 장시간 방은 메모리 단편화와 상태 누적을 방지하기 위해 30~60분 단위로 드레이닝 후 교체한다.

## 5.7 봇 설계
봇은 플레이어 수가 적은 방의 재미와 초기 매칭 속도를 보완하는 서버 측 에이전트다. 실제 사용자로 오인시키지 않도록 결과 화면 또는 정책에서 봇 포함 가능성을 고지한다.

| **영역**      | **요구사항**                                                    |
|---------------|-----------------------------------------------------------------|
| **목표 선택** | 가까운 먹이, 위험 회피, 공격 기회에 가중치 부여                 |
| **위험 감지** | 진행 방향의 충돌 예상 시간(TTC)과 주변 몸통 밀도 사용           |
| **난이도**    | 반응 지연, 시야 반경, 부스트 확률, 회전 오차로 조절             |
| **공정성**    | 서버의 전체 상태를 무제한 활용하지 않고 제한된 가상 시야를 사용 |
| **성능**      | 봇 판단은 5~10Hz, 이동은 같은 서버 틱에서 처리                  |

# 6. 기능 요구사항

| **ID**           | **우선순위** | **요구사항**                                                                         |
|------------------|--------------|--------------------------------------------------------------------------------------|
| **FR-AUTH-01**   | Must         | 시스템은 첫 방문 시 익명 사용자 ID와 갱신 가능한 게스트 토큰을 발급해야 한다.        |
| **FR-AUTH-02**   | Should       | 사용자는 게스트 진행도를 이메일 또는 소셜 계정에 연결할 수 있어야 한다.              |
| **FR-PROF-01**   | Must         | 닉네임은 길이, 문자 집합, 금칙어, 중복 허용 정책을 통과해야 한다.                    |
| **FR-MATCH-01**  | Must         | 시스템은 지연, 지역, 게임 모드, 방 수용량을 기준으로 방을 선택해야 한다.             |
| **FR-MATCH-02**  | Must         | 매칭 티켓에는 만료 시간과 1회용 게임 접속 토큰이 포함되어야 한다.                    |
| **FR-MATCH-03**  | Should       | 방이 부족하면 서버 봇이 목표 최소 인원을 보완해야 한다.                              |
| **FR-GAME-01**   | Must         | 서버는 이동, 부스트, 먹이, 충돌, 사망, 점수를 권위적으로 계산해야 한다.              |
| **FR-GAME-02**   | Must         | 클라이언트는 입력 시퀀스를 전송하고 서버 확인값으로 예측 오차를 보정해야 한다.       |
| **FR-GAME-03**   | Must         | 시스템은 스폰, 킬, 사망, 리더보드 변경 등 중요 이벤트를 신뢰성 있게 전달해야 한다.   |
| **FR-GAME-04**   | Should       | 사용자는 10초 이내 일시 단절 시 동일 경기로 재연결할 수 있어야 한다.                 |
| **FR-RANK-01**   | Must         | 게임 중 Top 10과 자신의 현재 순위를 표시해야 한다.                                   |
| **FR-RESULT-01** | Must         | 사망 후 순위, 점수, 길이, 생존 시간, 최고 기록 여부를 표시해야 한다.                 |
| **FR-COS-01**    | Must         | 사용자는 소유한 기본 스킨을 선택하고 모든 클라이언트에 표시할 수 있어야 한다.        |
| **FR-OPS-01**    | Must         | 운영자는 방 수용량, 틱, 봇 수, 밸런스 값을 환경/설정 버전으로 관리할 수 있어야 한다. |
| **FR-OPS-02**    | Should       | 운영자는 특정 버전을 단계적으로 배포하고 즉시 롤백할 수 있어야 한다.                 |
| **FR-ADMIN-01**  | Must         | 관리자는 활성 방, 인원, 평균 핑, 틱 지연, 오류율을 조회할 수 있어야 한다.            |
| **FR-SAFE-01**   | Must         | 신고, 닉네임 제재, 토큰 폐기, IP/계정 속도 제한 기능이 있어야 한다.                  |

## 6.1 기능별 상세 인수 조건

| **기능**      | **인수 조건**                                                                          |
|---------------|----------------------------------------------------------------------------------------|
| **게임 시작** | Play 선택 후 로딩 진행률이 표시되며, 15초 이상 지연 시 재시도/지역 변경 안내가 나온다. |
| **입력**      | 키보드와 포인터가 동시에 입력되어도 마지막 활성 장치를 기준으로 흔들림 없이 처리된다.  |
| **네트워크**  | 서버 스냅샷이 일시 누락되면 최대 250ms까지 보간하고 이후 연결 품질 경고를 표시한다.    |
| **사망**      | 한 경기의 사망 판정은 모든 관전자/플레이어에게 동일한 tickId와 원인으로 전달된다.      |
| **재연결**    | 연결 복구 시 새로운 세션을 만들지 않고 기존 playerId를 회수한다.                       |
| **결과 저장** | 게임 서버 장애가 있어도 결과 이벤트는 최소 1회 저장되고 중복은 matchId로 제거된다.     |
| **설정**      | 클라이언트와 서버의 configVersion이 다르면 입장을 차단하고 새로고침을 유도한다.        |

# 7. 화면·UX 요구사항
## 7.1 정보 구조

| **화면**        | **핵심 요소**                                         |
|-----------------|-------------------------------------------------------|
| **S-01 랜딩**   | 게임 로고, 짧은 설명, Play, 조작 안내, 정책 링크      |
| **S-02 로비**   | 닉네임, 스킨, Quick Play, 설정, 전적                  |
| **S-03 매칭**   | 지역, 예상 연결 상태, 취소, 로딩 진행                 |
| **S-04 게임**   | Canvas, 점수/길이, Top 10, 미니 상태, 연결 품질, 설정 |
| **S-05 결과**   | 순위, 점수, 생존, 킬, 최고 기록, 다시하기, 로비       |
| **S-06 설정**   | 음량, 그래픽 품질, 입력 방식, 접근성, 개인정보        |
| **S-07 프로필** | 전적, 보유 스킨, 미션/보상(Phase 2)                   |
| **S-08 관리자** | 서버 상태, 방 목록, 설정 배포, 제재, 공지             |

## 7.2 게임 HUD
- 좌상단: 현재 점수, 길이, 킬 수. 정보는 4Hz 이하로 업데이트하여 시각적 흔들림을 줄인다.

- 우상단: Top 10 리더보드와 본인 순위. 모바일에서는 접을 수 있다.

- 하단/중앙: 모바일 가상 조이스틱과 부스트 버튼. 데스크톱에서는 기본적으로 숨긴다.

- 상단 중앙: 서버 공지, 이벤트 타이머, 연결 품질 경고. 지속 노출을 피한다.

- 게임 오브젝트와 HUD는 색상 대비를 확보하고, 색상만으로 위험·팀·상태를 구분하지 않는다.

## 7.3 반응형·입력

| **환경**             | **조작**                                          | **레이아웃**                            |
|----------------------|---------------------------------------------------|-----------------------------------------|
| **Desktop ≥ 1024px** | 마우스 방향 또는 WASD/방향키, Space/좌클릭 부스트 | 16:9 중심, 전체 화면 지원               |
| **Tablet**           | 포인터/터치 조이스틱 선택                         | 가로 모드 권장                          |
| **Mobile**           | 왼쪽 조이스틱 + 오른쪽 부스트, 한손 모드 옵션     | 세로 진입 시 회전 안내 또는 축소형 지원 |
| **Gamepad (Could)**  | 왼쪽 스틱 방향, A/RT 부스트                       | 표준 Gamepad API                        |

## 7.4 접근성
- 색약 친화 팔레트와 몸통 외곽선 옵션을 제공한다.

- 화면 흔들림, 파티클, 배경 애니메이션, 플래시를 줄이는 옵션을 제공한다.

- 모든 로비/설정 UI는 키보드 탐색과 명확한 포커스 표시를 지원한다.

- 음향 신호만으로 전달되는 중요 정보에는 시각적 피드백을 함께 제공한다.

- 모바일 버튼 크기는 최소 44 CSS px 수준을 유지한다.

## 7.5 시각 방향
원작의 화면을 픽셀 단위로 모사하지 않는다. “밝은 아케이드 + 명확한 실루엣”만 장르 원칙으로 가져오고, 배경 테마·캐릭터 형태·HUD 구성은 독자 디자인한다.

| **요소**     | **가이드**                                               |
|--------------|----------------------------------------------------------|
| **아레나**   | 저채도 배경 + 약한 그리드/텍스처, 오브젝트 대비 확보     |
| **스네이크** | 원형/캡슐 세그먼트, 머리 표정과 패턴으로 식별            |
| **먹이**     | 크기·희귀도에 따라 형태를 달리하고 과도한 광원 효과 제한 |
| **사망**     | 짧은 디졸브 후 펠릿 전환, 화면 가림 최소화               |
| **브랜드**   | 자체 로고·명칭·색상 시스템, 상표 검색 후 확정            |

# 8. 기술 아키텍처
## 8.1 권장 기술 스택

| **계층**        | **권장안**                                                 | **역할**                                                     |
|-----------------|------------------------------------------------------------|--------------------------------------------------------------|
| **웹 앱**       | React + TypeScript + Vite                                  | 로비/프로필/설정/PWA 셸                                      |
| **2D 게임**     | Phaser                                                     | WebGL/Canvas 렌더링, 입력, 카메라, 오디오                    |
| **상태 관리**   | Zustand + TanStack Query                                   | 로컬 UI 상태와 REST 서버 상태 분리                           |
| **실시간 서버** | Node.js + TypeScript + Colyseus                            | 권위 상태, Room, 매치메이킹, 상태 동기화                     |
| **REST API**    | Fastify 또는 NestJS(Fastify adapter)                       | 인증·프로필·카탈로그·랭킹·운영                               |
| **DB**          | PostgreSQL                                                 | 영속 사용자/경제/전적/설정                                   |
| **캐시/조정**   | Redis                                                      | 세션, Presence, Room registry, rate limit, leaderboard cache |
| **자산**        | S3 호환 Object Storage + CDN                               | 스킨, 사운드, 이벤트 manifest                                |
| **관측성**      | OpenTelemetry + Prometheus/Grafana + Loki 또는 관리형 동급 | 메트릭·로그·트레이스                                         |
| **배포**        | Docker Compose(개발) → Managed Containers/Kubernetes(확장) | 환경 일관성과 수평 확장                                      |

Phaser는 웹 브라우저용 2D 게임에서 WebGL/Canvas와 TypeScript를 지원하고, Colyseus는 권위형 상태·방·매치메이킹·동기화를 제공하므로 MVP 구현 속도와 향후 확장성의 균형이 좋다. \[R4\]\[R5\]

## 8.2 논리 아키텍처
<img src="/mnt/data/snake_arena_prd_media/media/image1.png" style="width:6.49606in;height:2.59843in" />

*그림 1. 권장 논리 아키텍처*

## 8.3 아키텍처 원칙

| **원칙**                 | **적용**                                                        |
|--------------------------|-----------------------------------------------------------------|
| **Server authoritative** | 위치·충돌·점수는 서버가 최종 결정한다.                          |
| **Fixed timestep**       | 게임 시뮬레이션은 렌더 프레임과 분리된 고정 틱으로 실행한다.    |
| **Room isolation**       | 한 Room의 오류/과부하가 다른 Room으로 전파되지 않도록 격리한다. |
| **Stateless API**        | API 인스턴스는 세션 상태를 Redis/DB에 두고 수평 확장한다.       |
| **Config-driven**        | 밸런스와 콘텐츠는 버전된 설정으로 관리한다.                     |
| **Graceful degradation** | 분석/랭킹 장애가 실시간 경기를 중단시키지 않아야 한다.          |
| **Idempotency**          | 결과, 보상, 구매 등 중복 가능 작업은 멱등 키로 보호한다.        |

## 8.4 서비스 경계

| **컴포넌트**       | **책임**                                                  |
|--------------------|-----------------------------------------------------------|
| **web-client**     | 정적 SPA/PWA, Phaser 게임, REST/WSS 클라이언트            |
| **api-service**    | 인증, 프로필, 스킨, 설정 조회, 랭킹, 관리자 API           |
| **matchmaker**     | 큐, 지역 선택, Room 검색/생성, 접속 토큰 발급             |
| **game-server**    | Room별 시뮬레이션, 입력 검증, 상태 전송, 결과 이벤트 발행 |
| **result-worker**  | 경기 결과/보상 저장, 랭킹 갱신, 분석 이벤트 처리          |
| **admin-web**      | 운영 대시보드와 설정 배포 UI                              |
| **asset-pipeline** | 스킨 atlas, 압축 텍스처, manifest 버전 생성               |

# 9. 게임 서버 상세 설계
## 9.1 권위형 틱 파이프라인
<img src="/mnt/data/snake_arena_prd_media/media/image2.png" style="width:5.03937in;height:9.23395in" />

*그림 2. 입력부터 렌더링까지의 실시간 처리 흐름*

## 9.2 서버 틱 순서
6.  현재 tickId에 적용할 입력을 플레이어별로 하나 선택한다.

7.  입력 빈도, 방향 변화량, 부스트 가능 질량, 세션 상태를 검증한다.

8.  머리 위치와 경로 포인트를 고정 dt로 적분한다.

9.  공간 인덱스에 머리·몸통·펠릿 위치를 갱신한다.

10. 먹이 획득을 먼저 계산하고, 이어서 머리-몸통/머리-머리 충돌을 판정한다.

11. 사망, 킬 기여, 잔해 생성, 점수, 리더보드를 갱신한다.

12. 관심 영역과 우선순위에 따라 스냅샷/델타를 만든다.

13. 정해진 주기마다 결과/메트릭 이벤트를 비동기 발행한다.

## 9.3 상태 표현

| **객체**           | **주요 필드/설명**                                                                 |
|--------------------|------------------------------------------------------------------------------------|
| **PlayerState**    | id, nickname, head(x,y), angle, speed, mass, score, alive, skinId, lastAckInputSeq |
| **SnakePath**      | 최근 경로 키포인트 배열 또는 ring buffer; 렌더 세그먼트는 클라이언트가 복원        |
| **PelletState**    | id, x, y, value, type, spawnedAt                                                   |
| **RoomState**      | roomId, mode, configVersion, tickId, startedAt, leaderboard                        |
| **TransientEvent** | spawn, eat, boost, kill, death, notice; 짧은 TTL 또는 이벤트 채널                  |

## 9.4 공간 분할과 충돌 최적화
- 기본 구현은 셀 크기가 최대 충돌 반경보다 큰 Uniform Spatial Hash Grid를 사용한다.

- 머리 충돌은 현재 셀과 인접 8개 셀의 몸통 세그먼트만 검사한다.

- 몸통은 각 세그먼트를 캡슐 또는 원 체인으로 단순화하고, 먼저 AABB broad phase 후 정밀 거리를 계산한다.

- 펠릿 수가 많으면 셀별 배열과 swap-remove를 사용해 삭제 비용을 줄인다.

- 매 틱 전체 리더보드를 정렬하지 않고 점수 변경 플레이어만 갱신하거나 4Hz 주기로 계산한다.

- CPU 예산 초과 시 스냅샷 빈도/봇 판단/비핵심 파티클 이벤트를 줄이되 시뮬레이션 틱은 유지한다.

## 9.5 지연 보정

| **기법**               | **요구사항**                                                                             |
|------------------------|------------------------------------------------------------------------------------------|
| **Local prediction**   | 자기 머리는 입력 즉시 로컬에서 움직이고, 서버 확인 스냅샷으로 오차를 완만히 보정한다.    |
| **Interpolation**      | 다른 플레이어는 100~150ms 지연 버퍼에서 두 스냅샷 사이를 보간한다.                       |
| **Extrapolation**      | 스냅샷 누락 시 짧게 마지막 속도/각도로 외삽하되 250ms 이후 중단한다.                     |
| **Reconciliation**     | server lastAckInputSeq 이후의 미확인 입력을 재적용한다.                                  |
| **Collision fairness** | 서버 현재 상태 기준으로 판정하며 과도한 과거 되감기(lag compensation)는 적용하지 않는다. |
| **Clock sync**         | ping/pong으로 RTT와 offset을 추정하고 메시지에 serverTime/tickId를 포함한다.             |

## 9.6 Room 생명주기

| **상태**     | **동작**                                |
|--------------|-----------------------------------------|
| **Creating** | 설정 로드, 시드 생성, 초기 펠릿 배치    |
| **Open**     | 신규 사용자 입장 가능                   |
| **Full**     | 정원 도달, 신규 입장 차단               |
| **Draining** | 배포/노후화/장애 예고, 재입장만 허용    |
| **Closing**  | 남은 경기 결과 플러시, 신규 메시지 차단 |
| **Disposed** | Redis registry 제거, 메트릭 종료        |

## 9.7 장애 처리
- 게임 서버 프로세스가 종료되면 해당 경기는 복구하지 않는 것을 MVP 기본 정책으로 하되, 사용자에게 무효 경기 처리와 즉시 재매칭을 제공한다.

- 경기 결과 이벤트는 로컬 outbox 또는 메시지 큐에 기록한 후 비동기 저장하고, matchId/eventId로 중복을 제거한다.

- Redis 장애 시 기존 Room은 제한적으로 계속 실행하고 신규 매칭을 중단한다.

- PostgreSQL 장애 시 경기 플레이는 지속하되 결과를 큐에 보관하고 복구 후 반영한다.

- 이벤트 루프 지연이 임계치(예: P99 80ms)를 넘으면 해당 인스턴스를 드레이닝한다.

## 9.8 대형 맵 실시간 동기화 아키텍처

| **설계 전제 /** 아래 내용은 Snake.io의 비공개 서버 구현을 단정하는 분석이 아니라, 동일한 규모와 조작감을 목표로 하는 Serpent Arena의 권장 구현안이다. 대형 맵 전체를 모든 사용자에게 매 프레임 보내지 않고 Room 격리, 관심 영역(AOI), 서버 권위 판정과 클라이언트 보간을 결합한다. |
|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|

### 9.8.1 리전과 Room 단위 격리

- 전 세계 사용자를 하나의 시뮬레이션에 넣지 않는다. 매치메이커는 RTT, 서버 버전, Room 수용량과 게임 모드를 기준으로 사용자를 가장 가까운 리전의 Room에 배치한다.

- Room은 독립적인 메모리 상태와 틱 루프를 가지며, 한 Room의 과부하나 장애가 다른 Room으로 전파되지 않아야 한다.

- 맵의 가로·세로 크기는 네트워크 비용을 직접 결정하지 않는다. 사용자별 가시 범위 안에서 동기화되는 엔터티 수가 실제 비용을 결정한다.

- MVP 기준 Room 정원은 인간 30~60명과 선택적 봇 0~20명으로 시작하고, 60명 이상은 측정된 틱 P99와 사용자별 대역폭을 근거로 단계적으로 높인다.

```text
KR region JP region
game-01: Room A / Room B game-11: Room G / Room H
game-02: Room C / Room D game-12: Room I / Room J
↑ matchmaker selects by RTT, capacity and version
```

### 9.8.2 입력 중심 업로드와 서버 권위 시뮬레이션

클라이언트는 자신의 최종 좌표나 점수·길이를 보내지 않고 입력 순번, 방향과 부스트 여부만 보낸다. 서버는 고정 dt로 이동과 충돌을 계산하고 모든 결과를 최종 판정한다.

| **방향**      | **최소 필드**                                | **권장 빈도** | **처리 원칙**                             |
|---------------|----------------------------------------------|---------------|-------------------------------------------|
| C→S input     | seq, clientTime, angle 또는 direction, boost | 20Hz          | 최신 입력 우선. 좌표·점수는 수신하지 않음 |
| S→C snapshot  | tickId, serverTime, ackSeq, 주요 엔터티 상태 | 10Hz          | 초기/주기적 기준 상태                     |
| S→C delta     | changed, created, removed, events            | 10~20Hz       | 변경된 값만 전송                          |
| C↔S ping/pong | nonce, timestamps                            | 1~2Hz         | RTT와 시계 offset 추정                    |

- 서버는 입력의 초당 개수, 회전 속도, 부스트 가능 질량, sequence 역행, 비정상 메시지 크기를 검증한다. 입력 패킷은 유실되어도 다음 입력으로 대체할 수 있지만 death/result/config 메시지는 순서와 전달을 보장해야 한다.

### 9.8.3 관심 영역(AOI)과 엔터티 가시성

맵을 고정 크기 셀로 나누고 플레이어 주변의 현재 셀과 인접 셀만 구독한다. 서버는 AOI 안으로 들어온 엔터티를 enter, 나간 엔터티를 leave, 내부에서 변경된 엔터티를 delta로 전송한다.

| **구분**        | **초기값**          | **요구사항**                                                       |
|-----------------|---------------------|--------------------------------------------------------------------|
| Spatial cell    | 500 world units     | 최대 충돌 반경과 평균 화면 범위를 기준으로 프로파일링 후 조정      |
| Interest radius | 1,500 units         | 카메라 가시 범위보다 넓게 설정하여 진입 팝인을 방지                |
| Despawn radius  | 1,800 units         | interest radius보다 크게 두어 경계 왕복 시 잦은 enter/leave를 방지 |
| AOI entity cap  | 근접 뱀 30마리 목표 | 초과 시 거리·위험도·화면 포함 여부로 우선순위 부여                 |
| Pellet chunk    | 셀 단위             | 진입 시 목록/시드를 받고 이후 생성·제거 이벤트만 반영              |

- 리더보드와 킬 알림처럼 전역성이 필요한 정보는 별도 저빈도 채널로 전송하고, 멀리 있는 뱀의 위치·몸통은 전송하지 않는다.

### 9.8.4 클라이언트 예측·보간·보정

| **대상** | **표시 방식**         | **기준값**             | **오차 처리**                                                                 |
|----------|-----------------------|------------------------|-------------------------------------------------------------------------------|
| 내 뱀    | 입력 즉시 로컬 예측   | 입력 20Hz, 렌더 60FPS+ | 서버 ack 이후 미확인 입력을 재적용하고 작은 오차는 100~200ms 동안 완만히 보정 |
| 다른 뱀  | 과거 스냅샷 사이 보간 | 보간 버퍼 100ms        | 패킷 누락 시 최대 200ms 외삽 후 정지/감속                                     |
| 이벤트   | 서버 이벤트 기준      | tickId 정렬            | 사망·먹이 획득은 예측보다 서버 결과를 우선                                    |
| 카메라   | 내 예측 위치 추적     | 감쇠형 follow          | 서버 보정 시 카메라 순간이동을 피함                                           |

다른 플레이어는 실제 현재보다 약 100ms 과거의 상태를 표시한다. 지연을 제거하는 것이 아니라, 이미 수신한 두 상태 사이를 60FPS로 보간하여 지터와 패킷 간격을 숨긴다.

### 9.8.5 뱀 몸통 경로와 상태 압축

- 뱀의 모든 몸통 점을 매 스냅샷마다 전송하지 않는다. 서버는 머리가 지나간 경로 키포인트를 ring buffer로 관리하고 클라이언트는 길이와 세그먼트 간격을 이용해 몸통을 재구성한다.

- 방향 변화 또는 누적 이동 거리가 임계치를 넘을 때만 새 경로 키포인트를 추가한다. 오래된 경로는 현재 길이에 필요한 범위를 제외하고 제거한다.

- 초기 AOI 진입 시에는 기준 경로를 전송하고 이후에는 head 상태, length와 신규 path node만 델타로 전달한다.

- 좌표는 맵 원점을 기준으로 float64를 보내기보다 셀 로컬 정수 또는 float32로 양자화하고, 각도는 uint16으로 표현할 수 있다.

```text
input packet example (binary target)
type:uint8 | seq:uint16 | angle:uint16 | boost:uint8 ≈ 6 bytes payload
snake delta example
id | flags | headX | headY | angle | length? | newPathNode?
```

### 9.8.6 정적 먹이와 이벤트 동기화

- 정지한 먹이는 매 틱 위치를 전송하지 않는다. 사용자가 새 chunk에 진입할 때 현재 목록 또는 결정적 생성 seed를 제공하고 이후 created/removed 이벤트만 전송한다.

- 플레이어 사망으로 대량 잔해가 생성될 때는 개별 JSON 메시지를 연속 전송하지 않고 batch 이벤트 또는 seed+패턴으로 압축한다.

- 클라이언트의 먹이 렌더링 결과는 판정 근거가 아니며, 획득 여부와 질량 증가량은 서버가 결정한다.

### 9.8.7 공간 해시 기반 충돌 처리

모든 머리를 모든 몸통 점과 비교하지 않는다. 머리·몸통 캡슐·먹이를 spatial hash에 등록하고 현재 셀과 인접 셀 후보만 broad phase로 조회한 뒤 원-캡슐 거리 등 정밀 충돌을 수행한다.

| **단계**            | **처리**                                        | **성능 보호**                       |
|---------------------|-------------------------------------------------|-------------------------------------|
| 1\. Index update    | 이동한 머리와 새/제거된 몸통 세그먼트만 셀 갱신 | 전체 인덱스 재생성 금지             |
| 2\. Candidate query | 현재·인접 셀에서 후보 수집                      | 중복 ID 제거, 최대 후보 수 메트릭   |
| 3\. Broad phase     | AABB/반경으로 빠른 제외                         | 거리 제곱 비교로 sqrt 최소화        |
| 4\. Narrow phase    | 머리-캡슐, 머리-머리 판정                       | 서버 현재 tick을 최종 기준으로 사용 |
| 5\. Result batch    | 사망·잔해·점수 이벤트를 틱 끝에 일괄 반영       | 반복 제거와 순서 의존 버그 방지     |

### 9.8.8 대역폭 예산과 적응형 전송

| **항목**               | **목표/정책**                                                         |
|------------------------|-----------------------------------------------------------------------|
| 사용자 평균 트래픽     | 송수신 합계 40KB/s 이하 목표. 실제 값은 AOI 밀도와 긴 뱀 비율로 측정  |
| 입력 업로드            | 20Hz. 변경이 없더라도 heartbeat 성격의 최신 입력을 제한된 주기로 전송 |
| 근접 뱀                | 10~20Hz head/path delta                                               |
| 먼 거리·화면 밖 엔터티 | 전송 제외 또는 2~5Hz 저빈도 요약                                      |
| 리더보드               | 4Hz 이하 또는 변경 시 batch                                           |
| 혼잡 시                | 시뮬레이션 20Hz는 유지하고 snapshot, 봇 AI, 비핵심 이벤트 순으로 낮춤 |

서버는 사용자별 bytesOut, entitiesInAOI, snapshotSize P95, correctionDistance, droppedSnapshot과 tickDuration을 함께 관측해야 한다. 네트워크 절감으로 클라이언트 보정이 과도해지지 않는지 같이 평가한다.

### 9.8.9 단계별 구현 기준

| **단계** | **동시 사용자/Room** | **통신**                                  | **필수 최적화**                                        |
|----------|----------------------|-------------------------------------------|--------------------------------------------------------|
| MVP      | 30명 + 봇            | JSON WebSocket, 20Hz tick / 10Hz snapshot | 서버 권위, uniform grid, 100ms 보간, 기본 AOI          |
| Beta     | 60명 + 봇            | MessagePack 또는 schema binary, delta     | 경로 키포인트, AOI enter/leave, 좌표 양자화            |
| Scale    | 60~100명 검토        | 우선순위·적응형 전송                      | 긴 몸통 worst-case, chunk seed, Room별 CPU/egress 예산 |

# 10. 프런트엔드 상세 설계
## 10.1 React와 Phaser 경계

| **영역**           | **책임**                                                                                |
|--------------------|-----------------------------------------------------------------------------------------|
| **React 담당**     | 라우팅, 로비, 프로필, 설정, 모달, 접근성, REST 데이터, 인증                             |
| **Phaser 담당**    | 게임 렌더링, 카메라, 입력 샘플링, 오디오, 파티클, 로컬 예측/보간                        |
| **Bridge**         | 타입이 지정된 Event Bus 또는 adapter; game state 전체를 React에 매 프레임 저장하지 않음 |
| **Zustand**        | 닉네임, 스킨, 설정, 연결 상태, 결과 등 낮은 빈도의 공유 상태                            |
| **TanStack Query** | 프로필, 카탈로그, 랭킹, 전적 등 서버 캐시                                               |

## 10.2 렌더링 요구사항
- 기본 목표는 60 FPS, 저사양/배터리 절약 모드는 30 FPS를 허용한다.

- 카메라는 자기 머리를 부드럽게 추적하고 길이에 따라 제한된 범위에서 줌 아웃한다.

- 화면 밖 객체는 렌더하지 않으며, 몸통은 객체 풀링과 배치 가능한 sprite/mesh 방식을 사용한다.

- 펠릿·파티클은 object pool을 사용하고 매 프레임 메모리 할당을 최소화한다.

- 고해상도 기기에서 devicePixelRatio 상한을 설정해 GPU fill rate를 보호한다.

- 탭 비활성화 시 렌더링을 감속하되 네트워크 heartbeat와 재연결 정책은 유지한다.

## 10.3 클라이언트 상태 머신

| **상태**         | **설명**                                           |
|------------------|----------------------------------------------------|
| **BOOT**         | 설정/manifest/폰트/필수 자산 로드                  |
| **LOBBY**        | 사용자 입력과 스킨 선택                            |
| **MATCHING**     | 티켓 요청 및 WSS 접속 준비                         |
| **JOINING**      | handshake, config/version 검증, 초기 snapshot 수신 |
| **PLAYING**      | 입력 전송, 예측, 보간, 렌더                        |
| **RECONNECTING** | 재연결 토큰으로 지수 백오프 시도                   |
| **RESULT**       | 경기 결과 표시/저장 확인                           |
| **ERROR**        | 복구 가능한 오류와 치명 오류 분리 안내             |

## 10.4 자산 파이프라인
- 스킨 이미지는 texture atlas로 묶고 manifest에 hash/version/size를 기록한다.

- 초기 번들에는 기본 스킨과 공통 효과만 포함하고 이벤트 자산은 지연 로드한다.

- WebP/AVIF 지원 여부를 감지하고 폴백 이미지를 제공한다.

- 오디오는 사용자 상호작용 이후 활성화하고, 브라우저 자동재생 정책을 준수한다.

- 서비스 워커는 정적 자산을 cache-first로 제공하되 게임 API/WSS를 캐시하지 않는다.

## 10.5 오류 UX

| **상황**                | **표시/행동**                                     |
|-------------------------|---------------------------------------------------|
| **Unsupported browser** | WebGL/Canvas 지원 및 최신 브라우저 안내           |
| **Version mismatch**    | 새 버전 감지, 경기 전 새로고침                    |
| **Match timeout**       | 지역 변경 또는 재시도                             |
| **High latency**        | 연결 품질 아이콘, 자동 지역 재매칭은 경기 종료 후 |
| **Disconnected**        | 10초 재연결 카운트다운, 실패 시 무효/결과 안내    |
| **Server maintenance**  | 종료 시각/예상 재개 시간, 진행 중 방 드레이닝     |

# 11. REST API와 WebSocket 프로토콜
## 11.1 REST API 원칙
- /v1 기반 버전 경로, JSON, UTC ISO-8601 시간, UUID/ULID 식별자를 사용한다.

- 게스트와 계정 모두 Bearer access token을 사용하고 refresh token은 HttpOnly Secure cookie 또는 안전한 저장 정책을 적용한다.

- 쓰기 요청은 Idempotency-Key를 지원하고 표준 오류 코드(code, message, requestId)를 반환한다.

- OpenAPI 스키마로 클라이언트 타입과 테스트 mock을 생성한다.

- 관리자 API는 별도 도메인/권한과 강한 인증을 사용한다.

## 11.2 주요 REST 엔드포인트

| **Method** | **Path**                 | **설명**                      |
|------------|--------------------------|-------------------------------|
| **POST**   | /v1/auth/guest           | 게스트 세션 생성              |
| **POST**   | /v1/auth/refresh         | access token 갱신             |
| **POST**   | /v1/auth/link            | 게스트를 계정에 연결          |
| **GET**    | /v1/me                   | 프로필/설정/전적 조회         |
| **PATCH**  | /v1/me                   | 닉네임/설정 수정              |
| **GET**    | /v1/cosmetics            | 스킨 카탈로그와 소유 상태     |
| **PUT**    | /v1/me/loadout           | 선택 스킨 저장                |
| **POST**   | /v1/matches/tickets      | Quick Match 티켓 발급         |
| **GET**    | /v1/leaderboards/{scope} | 일/주/전체 랭킹               |
| **GET**    | /v1/config/client        | 클라이언트 공개 설정/manifest |
| **POST**   | /v1/reports              | 신고 제출                     |
| **POST**   | /v1/telemetry/batch      | 비핵심 클라이언트 분석 이벤트 |

## 11.3 매칭 티켓 예시

```http
POST /v1/matches/tickets
{
"mode": "classic",
"preferredRegion": "auto",
"clientVersion": "1.0.0",
"configVersion": "2026-07-13.1"
}
200 OK
{
"ticketId": "01J...",
"roomId": "kr-seoul-42",
"endpoint": "wss://game-kr.example.com/rooms/kr-seoul-42",
"joinToken": "eyJ...",
"expiresAt": "2026-07-13T00:00:15Z"
}
```

## 11.4 WebSocket 메시지

| **방향** | **메시지**            | **주요 필드**                                                      |
|----------|-----------------------|--------------------------------------------------------------------|
| C→S      | hello                 | joinToken, clientVersion, configVersion, resumeToken?              |
| S→C      | welcome               | playerId, roomId, tickRate, snapshotRate, serverTime, initialState |
| C→S      | input                 | seq, clientTime, direction(x,y), boost                             |
| S→C      | snapshot              | tickId, serverTime, players, pellets, lastAckInputSeq              |
| S→C      | delta                 | tickId, changed/removed entities                                   |
| S→C      | event                 | spawn/eat/kill/death/notice payload                                |
| S→C      | leaderboard           | entries, selfRank                                                  |
| C↔S      | ping/pong             | nonce, timestamps                                                  |
| S→C      | result                | matchId, rank, score, length, survivalMs, kills, reason            |
| S→C      | error                 | code, recoverable, message                                         |
| C→S      | leave                 | reason                                                             |
| S→C      | aoi_enter / aoi_leave | entity baseline 또는 entityId, reason                              |
| S→C      | snake_delta           | flags, head, angle, length?, pathNode?                             |
| C→S      | resync                | lastGoodTickId, reason                                             |

## 11.5 프로토콜 규칙
- MVP 개발 초기에는 JSON을 사용할 수 있으나, 부하 테스트 후 MessagePack/Schema 기반 바이너리로 전환한다.

- 모든 메시지는 type, protocolVersion, roomId, tickId 또는 sequence를 식별할 수 있어야 한다.

- 입력 메시지는 드롭 가능하고 최신값 우선이며, death/result/config/error는 신뢰성 있는 순서 보장을 요구한다.

- 클라이언트가 보낸 좌표, 점수, 길이, 먹이 획득 결과는 절대 신뢰하지 않는다.

- 서버는 초당 입력 수, 방향 변화량, 메시지 크기, 잘못된 schema 횟수를 제한한다.

- 프로토콜 변경은 backward compatible 기간 또는 강제 버전 갱신 정책을 문서화한다.

## 11.6 AOI·델타·바이너리 프로토콜 요구사항

| **메시지**    | **핵심 필드**                               | **설명**                                     |
|---------------|---------------------------------------------|----------------------------------------------|
| aoi_enter     | entityId, entityType, baselineState/path    | 새 관심 영역에 들어온 엔터티의 기준 상태     |
| aoi_leave     | entityId, reason                            | 관심 영역 이탈. 즉시 삭제보다 짧은 fade 허용 |
| snake_delta   | id, flags, head, angle, length?, pathNode?  | flags에 포함된 필드만 해석                   |
| pellet_batch  | chunkId, created\[\], removed\[\] 또는 seed | 정적 먹이의 변화만 batch 동기화              |
| snapshot_meta | tickId, serverTime, ackSeq, configVersion   | 보간·재조정과 호환성 검증 기준               |

- 초기 연결과 재접속 기준 상태는 full snapshot으로 제공하고, 이후에는 delta를 기본으로 한다. delta 손실 또는 기준 불일치가 감지되면 resync 요청으로 새 baseline을 받는다.

- 바이너리 전환 시 메시지마다 protocolVersion과 schemaVersion을 포함하거나 연결 handshake에서 합의한다. 알 수 없는 선택 필드는 무시할 수 있어야 한다.

- 정수 양자화 오차는 충돌 판정에 사용하지 않는다. 충돌은 서버 내부 정밀 좌표로 계산하고, 양자화는 네트워크 표현에만 적용한다.

# 12. 데이터 모델
## 12.1 핵심 엔터티

| **테이블**              | **주요 컬럼**                                                              |
|-------------------------|----------------------------------------------------------------------------|
| **users**               | id, type(guest/account), status, created_at, last_seen_at                  |
| **identities**          | user_id, provider, provider_subject, verified_at                           |
| **profiles**            | user_id, nickname, locale, region, selected_skin_id, settings_json         |
| **cosmetics**           | id, type, rarity, asset_key, unlock_rule, active                           |
| **user_cosmetics**      | user_id, cosmetic_id, acquired_at, source                                  |
| **matches**             | id, room_id, mode, region, config_version, started_at, ended_at, status    |
| **match_players**       | match_id, user_id, rank, score, max_mass, survival_ms, kills, death_reason |
| **leaderboard_entries** | scope, period_key, user_id, score, rank_metadata                           |
| **missions**            | id, period, condition_json, reward_json, start_at, end_at                  |
| **user_missions**       | user_id, mission_id, progress, claimed_at                                  |
| **game_configs**        | version, payload_json, status, created_by, activated_at                    |
| **moderation_actions**  | id, user_id, action, reason, expires_at, actor_id                          |
| **reports**             | id, reporter_id, target_id, match_id, reason, status                       |

## 12.2 데이터 원칙
- 실시간 Room 상태와 펠릿/몸통 세그먼트는 PostgreSQL에 매 틱 저장하지 않는다.

- 영속 저장은 경기 종료 요약과 중요한 감사 이벤트로 제한한다.

- 게스트 ID도 사용자 테이블에 존재하되 개인정보 없이 토큰 주체로 사용한다.

- 닉네임은 표시값과 정규화값을 분리하고 금칙어 버전을 기록한다.

- 스킨/보상 지급은 unique(user_id, cosmetic_id)와 멱등 이벤트 ID로 중복을 차단한다.

- 원시 분석 이벤트는 별도 분석 저장소 또는 장기 Object Storage로 내보낼 수 있다.

## 12.3 보존 정책

| **데이터**             | **예시 보존**                   | **주의**                          |
|------------------------|---------------------------------|-----------------------------------|
| **게임 서버 로그**     | 7~14일 hot, 오류 샘플 장기 보관 | 개인 토큰/전체 IP는 마스킹        |
| **경기 요약**          | 서비스 운영 기간 + 정책         | 삭제 요청 시 사용자 식별 해제     |
| **원시 텔레메트리**    | 30~90일                         | 목적과 동의 범위에 맞게 최소 수집 |
| **신고/제재**          | 분쟁 대응 기간                  | 접근 권한과 감사 로그 필요        |
| **게스트 미사용 계정** | 예: 180일 후 삭제/익명화        | 정책 고지                         |

# 13. 인프라·배포·운영
## 13.1 환경 구성

| **환경**       | **구성**                                                 | **목적**            |
|----------------|----------------------------------------------------------|---------------------|
| **local**      | Docker Compose: PostgreSQL, Redis, API, game server, web | 개발자 1명 실행     |
| **dev**        | 공유 개발 환경, 단일 지역, 저용량                        | 통합 테스트/QA      |
| **staging**    | 운영과 동일 토폴로지의 축소판                            | 부하/배포/장애 훈련 |
| **production** | 다중 AZ, 최소 2개 API/매치메이커, 게임 서버 오토스케일   | 실서비스            |

## 13.2 초기 프로덕션 서버 권장안

> **주의**
>
> 아래 사양은 60명/Room, 20Hz의 출발점입니다. 실제 수용량은 봇 수, 몸통 길이, 펠릿 수, 바이너리 프로토콜 여부에 크게 좌우되므로 출시 전 soak/load test 결과로 확정합니다.

| **계층**          | **초기 사양**                    | **용도**                               |
|-------------------|----------------------------------|----------------------------------------|
| **CDN/정적 웹**   | 관리형 CDN + Object Storage      | 정적 자산, 캐시, TLS                   |
| **Load Balancer** | L7, WebSocket 지원, 2개 AZ       | API/WSS 라우팅, 헬스 체크              |
| **API**           | 2 vCPU / 2~4GB × 2               | 무상태 REST, 관리자 API 분리 가능      |
| **Matchmaker**    | 1~2 vCPU / 2GB × 2               | Room registry, 티켓 발급               |
| **Game server**   | 4 vCPU / 8GB 인스턴스 풀, 최소 2 | 프로세스당 Room 수는 CPU 예산으로 제한 |
| **PostgreSQL**    | 2~4 vCPU / 8GB, Multi-AZ 권장    | 계정/전적/설정                         |
| **Redis**         | 1~2GB managed primary+replica    | 세션/Presence/Rate limit               |
| **Worker**        | 1~2 vCPU / 2GB × 1~2             | 결과 저장, 랭킹, 이벤트                |
| **Observability** | 관리형 또는 별도 노드            | 메트릭/로그/트레이스/알림              |

## 13.3 게임 서버 오토스케일
- CPU만으로 확장하지 않고 activeRooms, activePlayers, eventLoopLag, tickDurationP95, roomSlotsAvailable 같은 커스텀 메트릭을 사용한다.

- 새 게임 서버는 Ready 상태가 된 뒤 Redis registry에 capacity를 게시한다.

- 축소 시 즉시 종료하지 않고 Draining으로 전환하여 신규 Room을 받지 않으며 기존 Room이 종료된 뒤 제거한다.

- 예측 가능한 이벤트 시작 전 scheduled pre-scale을 사용한다.

- Kubernetes를 사용할 경우 HPA는 CPU/메모리 및 커스텀 메트릭에 따라 replica 수를 조정할 수 있다. \[R6\]

## 13.4 네트워크 라우팅

| **영역**      | **정책**                                                                         |
|---------------|----------------------------------------------------------------------------------|
| **지역 선택** | 클라이언트에서 후보 region ping 측정 + IP 지리정보 보조                          |
| **WSS 종료**  | Load Balancer 또는 게임 서버; 중간 idle timeout을 heartbeat보다 충분히 크게 설정 |
| **세션 고정** | 티켓의 room endpoint로 직접 연결하여 일반적인 sticky cookie 의존 최소화          |
| **DDoS**      | CDN/WAF, 연결/메시지 rate limit, 지역별 동시 연결 상한                           |
| **배포**      | 신버전 서버 풀 생성 → 신규 Room만 할당 → 구버전 드레이닝                         |

### 13.4.1 실시간 트래픽 경로와 데이터 배치

- REST API는 로그인·프로필·매칭 티켓에 사용하고, 경기 중 입력과 상태는 Room endpoint의 단일 WebSocket 연결로 처리한다.

- 게임 상태는 게임 서버 프로세스 메모리에 두며, 매 틱 Redis나 PostgreSQL에 좌표를 기록하지 않는다. Redis는 Room registry, presence, 재접속 토큰과 용량 정보에 사용한다.

- 로드밸런서 idle timeout은 heartbeat 간격보다 충분히 크게 설정하고, WebSocket 압축(permessage-deflate)은 작은 고빈도 바이너리 패킷에서 CPU 이득을 측정한 뒤 선택한다.

- 리전 간 실시간 Room 상태 복제는 MVP 범위에서 제외한다. Room 장애 시 경기 무효 처리와 신속한 재매칭을 제공하는 편이 복잡도와 비용 측면에서 유리하다.

## 13.5 CI/CD
- PR: lint, typecheck, unit test, protocol compatibility test, build, dependency/license scan.

- main: 컨테이너 이미지와 정적 자산을 immutable tag로 빌드하고 SBOM을 생성한다.

- staging: smoke + bot load + schema migration dry run + Playwright E2E.

- production: canary API 배포, 게임 서버 신규 Room 전용 배포, 메트릭 확인 후 점진 확장.

- DB migration은 backward compatible한 expand/contract 순서를 사용하며 즉시 롤백 계획을 가진다.

- 클라이언트, 서버, protocol, game config 버전을 배포 메타데이터에 함께 기록한다.

## 13.6 관측성과 알림

| **분류**      | **핵심 지표**                                                               |
|---------------|-----------------------------------------------------------------------------|
| **제품**      | DAU, 경기 시작률, 평균 세션, 재도전률, D1/D7                                |
| **매칭**      | ticket latency, room join latency, match failures, region distribution      |
| **게임 품질** | RTT P50/P95, packet/message loss proxy, reconnect rate, correction distance |
| **서버**      | tick duration P95/P99, event loop lag, active rooms/players, memory, GC     |
| **API**       | RPS, latency, error rate, DB pool saturation                                |
| **비즈니스**  | 스킨 선택률, 미션 완료율, 이벤트 참여율(Phase 2+)                           |

| **등급** | **조건 예시**                                    | **조치**               |
|----------|--------------------------------------------------|------------------------|
| **P1**   | 게임 입장 성공률 \< 90% 또는 다수 지역 전체 장애 | 즉시 호출/롤백         |
| **P1**   | tickDuration P99 \> tick budget가 5분 지속       | 드레이닝·확장          |
| **P2**   | P95 RTT/재연결률 급증                            | 지역/통신사 분석       |
| **P2**   | 결과 저장 backlog 임계 초과                      | worker/DB 조치         |
| **P3**   | 클라이언트 오류율 증가                           | 릴리즈/브라우저별 분석 |

# 14. 보안·부정행위 대응
## 14.1 위협 모델

| **위협**            | **예시**                           | **통제**                                          |
|---------------------|------------------------------------|---------------------------------------------------|
| **클라이언트 변조** | 속도·좌표·점수·스킨 소유 조작      | 서버 권위, 입력만 수신, 소유권 서버 검증          |
| **입력 봇/매크로**  | 완벽한 회피·과도한 입력            | 입력 빈도/패턴 분석, 제재는 보수적으로            |
| **프로토콜 남용**   | 대형 메시지, invalid schema, flood | 메시지 크기/초당 횟수 제한, 연결 차단             |
| **토큰 탈취**       | 게스트/계정 세션 도용              | 짧은 access token, refresh 회전, TLS, 로그 마스킹 |
| **닉네임 악용**     | 욕설, 사칭, 개인정보               | 금칙어, 길이/문자 제한, 신고/변경 이력            |
| **결과 재전송**     | 보상/전적 중복                     | matchId/eventId 멱등성                            |
| **관리자 침해**     | 설정 조작/대량 제재                | MFA, RBAC, 별도 도메인, 감사 로그                 |
| **DDoS**            | 연결/대역폭 고갈                   | CDN/WAF, 연결 제한, 지역 격리, 오토스케일         |

## 14.2 입력 검증 규칙
- 방향 벡터는 정규화하고 NaN/Infinity/비정상 범위를 거부한다.

- 클라이언트 seq는 단조 증가해야 하며 지나치게 오래된/미래 입력을 폐기한다.

- 부스트는 서버의 현재 질량과 쿨다운으로 판정한다.

- 최대 회전 속도보다 큰 방향 변화도 서버가 제한한다.

- 접속 토큰은 roomId, userId, expiry, nonce에 바인딩하고 1회 사용 또는 짧은 TTL을 적용한다.

- 반복 위반은 점수 무효화보다 먼저 연결 종료·쿨다운·관찰 플래그를 적용한다.

## 14.3 개인정보·아동 보호 고려
- 게스트 플레이를 기본으로 하여 이메일/전화번호 수집을 최소화한다.

- 채팅을 제공하지 않는 MVP는 아동 사용자 간 직접 상호작용 위험을 크게 줄인다.

- 광고·결제·아동 대상 서비스로 확장할 경우 출시 국가의 개인정보·아동 보호·소비자 규정을 별도 법률 검토한다.

- 분석 SDK, 광고 SDK, 쿠키는 목적·동의·보존 정책과 함께 검토하고 불필요한 추적을 배제한다.

- 계정 삭제/데이터 삭제 요청 흐름과 지원 채널을 제공한다.

# 15. 비기능 요구사항

| **ID**       | **분류**    | **기준**                                                               |
|--------------|-------------|------------------------------------------------------------------------|
| NFR-PERF-01  | 게임 렌더링 | 중급 모바일/데스크톱에서 P95 50~60FPS, 저사양 모드 ≥ 30FPS             |
| NFR-PERF-02  | 서버 틱     | 20Hz 기준 tickDuration P99 \< 45ms, 목표 \< 35ms                       |
| NFR-NET-01   | 트래픽      | 평균 게임 수신+송신 목표 40KB/s 이하/사용자, 부하 테스트로 확정        |
| NFR-LAT-01   | 매칭        | P95 티켓 발급+Room join ≤ 5초                                          |
| NFR-REL-01   | 가용성      | 월간 API/매칭 99.9% 목표; 단일 Room 장애 격리                          |
| NFR-SCALE-01 | 확장        | 게임 서버 인스턴스 추가로 선형에 가까운 동시접속 확장                  |
| NFR-SEC-01   | 보안        | TLS, 최소 권한, 비밀 관리, dependency scan                             |
| NFR-COMP-01  | 호환성      | 최근 주요 Chrome/Edge/Safari/Firefox 및 Android/iOS 웹뷰 범위 정의     |
| NFR-OBS-01   | 추적성      | 모든 요청/경기에 requestId, userId(가명), matchId, roomId 연계         |
| NFR-MAINT-01 | 유지보수    | 공유 game-core/protocol 타입, 설정 버전, 자동 테스트                   |
| NFR-NET-02   | AOI         | 사용자에게 가시 범위+여유 반경 엔터티만 전송하며 AOI 엔터티 P95를 계측 |
| NFR-NET-03   | 스냅샷      | 20Hz 시뮬레이션에서 10Hz 기본 전송, 혼잡 시 비핵심 대상만 저빈도화     |
| NFR-LAT-02   | 화면 보정   | 일반 네트워크에서 위치 보정 거리 P95가 캐릭터 반경의 0.5배 이하 목표   |
| NFR-SCALE-02 | Room 격리   | 단일 Room CPU 폭주·종료가 다른 Room과 API에 연쇄 장애를 일으키지 않음  |

## 15.1 용량 계획 공식
서버 한 대당 Room 수는 CPU가 아닌 “최악 조건 틱 예산”으로 결정한다. 다음 방식으로 부하 테스트 결과를 계산한다.

```text
room_cpu_budget_ms = tick_interval_ms × target_cpu_utilization
max_rooms_by_cpu = floor(instance_cores × room_cpu_budget_ms / measured_room_tick_p99_ms)
예: 20Hz → tick interval 50ms, 목표 CPU 60%
측정된 60명 Room tick P99가 8ms라면,
4 core 인스턴스의 이론값은 floor(4 × 30 / 8) = 15 rooms
운영값은 GC·네트워크·버스트 여유를 두어 이보다 낮게 설정한다.
```

## 15.2 브라우저 성능 예산

| **항목**           | **예산**                  | **비고**                  |
|--------------------|---------------------------|---------------------------|
| **초기 JS/CSS**    | gzip 기준 500KB 내외 목표 | 게임 엔진 분할 로딩 포함  |
| **첫 화면 자산**   | 1.5MB 이하 목표           | 기본 스킨/폰트/로고       |
| **추가 게임 자산** | 필요 시 지연 로드         | 이벤트·고급 스킨          |
| **메모리**         | 모바일 250MB 이하 목표    | 장시간 플레이 누수 테스트 |
| **프레임 CPU**     | 60FPS 기준 평균 12ms 이하 | 렌더+게임 로직 합계       |
| **GPU draw calls** | 프로파일링으로 상한 관리  | atlas/batching 사용       |

# 16. 분석·KPI·라이브 운영
## 16.1 핵심 퍼널

| **이벤트**         | **정의**           |
|--------------------|--------------------|
| **landing_view**   | 랜딩 노출          |
| **play_click**     | Play 선택          |
| **guest_created**  | 게스트 생성 성공   |
| **ticket_created** | 매칭 티켓 성공     |
| **room_joined**    | 초기 snapshot 수신 |
| **first_input**    | 첫 입력            |
| **match_ended**    | 사망/종료          |
| **retry_click**    | 재도전             |
| **account_linked** | 계정 연결          |

## 16.2 북극성 지표와 보조 지표

| **분류**       | **지표**                                              | **해석**             |
|----------------|-------------------------------------------------------|----------------------|
| **North Star** | 주간 활성 사용자당 완료 경기 수                       | 코어 플레이 반복성   |
| **Activation** | 첫 방문→첫 경기 입장률                                | 온보딩/매칭 문제     |
| **Engagement** | 평균 경기 수, 재도전률, 평균 세션                     | 재미와 마찰          |
| **Retention**  | D1/D7 재방문                                          | 콘텐츠/진행도        |
| **Quality**    | Room join 성공률, RTT, reconnect, correction distance | 네트워크 품질        |
| **Fairness**   | 의심 입력률, 신고율, 무효 경기율                      | 신뢰                 |
| **Live Ops**   | 이벤트 참여/완료, 스킨 획득/장착                      | Phase 2+ 콘텐츠 효율 |

## 16.3 라이브 설정

| **설정군**            | **항목**                                                 |
|-----------------------|----------------------------------------------------------|
| **GameConfig**        | 속도, 부스트, 충돌 반경, 펠릿 밀도, Room 정원, 봇 난이도 |
| **Feature flags**     | 계정 연결, 미션, 이벤트 HUD, 신규 프로토콜               |
| **Content manifest**  | 스킨, 로컬라이즈 카피, 자산 URL/hash                     |
| **Event schedule**    | 시작/종료, 모드, 맵 테마, 보상, 대상 지역                |
| **Moderation config** | 금칙어 버전, 제재 기간, rate limit                       |
| **Rollout**           | 내부→1%→10%→50%→100%, 자동 중단 지표                     |

# 17. 테스트 전략
## 17.1 테스트 계층

| **유형**                 | **범위**                                                     |
|--------------------------|--------------------------------------------------------------|
| **Unit**                 | 벡터/회전, 몸통 샘플링, 충돌, 점수, config validation        |
| **Property/Fuzz**        | NaN, 극단 좌표, 메시지 순서, 입력 폭주, collision invariants |
| **Protocol**             | 서버/클라이언트 schema 호환, 이전 버전 거부/허용             |
| **Integration**          | 매칭→입장→플레이→결과 저장, Redis/DB 장애                    |
| Deterministic simulation | 동일 seed+입력 로그가 동일 tick 결과 생성                    |
| **E2E**                  | 브라우저 Playwright로 게스트→매칭→게임→결과                  |
| **Load**                 | 봇 클라이언트로 Room 수/플레이어/몸통 길이/펠릿 최악 조건    |
| **Soak**                 | 6~24시간 장기 실행으로 메모리/GC/Room 교체 검증              |
| **Chaos**                | 프로세스 종료, Redis/DB 지연, 패킷 지연/손실, 배포 드레이닝  |
| **Visual/Device**        | 해상도, DPI, 터치, 모바일 브라우저, 저사양 GPU               |

## 17.2 필수 부하 시나리오

| **ID** | **조건**                                  | **검증**                                           |
|--------|-------------------------------------------|----------------------------------------------------|
| L-01   | 60명 + 20봇, 평균 길이, 기본 펠릿         | 1 Room 기준 tick/대역폭                            |
| L-02   | 60명, 상위 10명이 매우 긴 몸통            | 충돌 broad phase 최악                              |
| L-03   | 다수 연속 사망/잔해 생성                  | 객체 생성/삭제 burst                               |
| L-04   | 인스턴스당 5→10→15 Room 단계 증가         | 수용량 곡선                                        |
| L-05   | 1,000명 동시 Play 클릭                    | 매칭/API/Redis burst                               |
| L-06   | 5% packet loss + 200ms RTT                | 보간/재연결 UX                                     |
| L-07   | 배포 중 Room drain                        | 무중단 신규 경기 전환                              |
| L-08   | 20,000×20,000 맵, 플레이어 군집/분산 반복 | AOI enter/leave churn, 사용자별 엔터티·대역폭 상한 |
| L-09   | 상위 5명이 최대 길이, 경로 키포인트 누적  | 경로 메모리, delta 크기, 클라이언트 재구성 FPS     |
| L-10   | snapshot 10% burst loss + 100ms jitter    | 보간 버퍼, 외삽 중단, correction distance          |
| L-11   | 한 셀에 30명과 잔해 대량 밀집             | spatial hash 후보 수, narrow phase P99             |

## 17.3 릴리즈 게이트
- 치명/높음 등급 보안 취약점 0건 또는 승인된 예외.

- 핵심 브라우저 E2E 100% 통과.

- 목표 동시접속의 1.5배 부하에서 오류율과 틱 예산 충족.

- 6시간 soak에서 지속 메모리 증가 없음.

- 배포/롤백/드레이닝 리허설 완료.

- 개인정보·이용약관·신고/지원 흐름 검토 완료.

- 대시보드와 P1/P2 알림이 실제로 작동함을 확인.

# 18. 개발 로드맵
## 18.1 1인 중심 MVP 예시 일정
아래는 숙련된 TypeScript 개발자 1명과 파트타임 아트/사운드 지원을 가정한 14~18주 기준안이다. 멀티플레이 경험, 자산 품질, 운영 요건에 따라 달라질 수 있다.

| **단계**                  | **기간** | **산출물**                                            |
|---------------------------|----------|-------------------------------------------------------|
| **0. Discovery**          | 1주      | 브랜드/차별화, 프로토타입 수치, 기술 spike, 법적 경계 |
| **1. Offline Core**       | 2주      | Phaser 이동/몸통/먹이/충돌/카메라/모바일 입력         |
| **2. Authoritative Room** | 3주      | Colyseus Room, 고정 틱, 입력, snapshot, 보간          |
| **3. Match & Identity**   | 2주      | 게스트, API, Redis registry, ticket, 재연결           |
| **4. Product UI**         | 2주      | 랜딩/로비/HUD/결과/설정/기본 스킨                     |
| **5. Persistence & Ops**  | 2주      | 전적, 결과 worker, dashboard, config, logs/metrics    |
| **6. Hardening**          | 2~3주    | 부하/soak/보안/브라우저/네트워크 테스트               |
| **7. Beta & Launch**      | 1~3주    | 소규모 공개, 밸런스, 비용/용량 조정, 출시             |

## 18.2 마일스톤 종료 조건

| **마일스톤**         | **종료 조건**                                                      |
|----------------------|--------------------------------------------------------------------|
| **M1 오프라인 재미** | 로컬 10분 플레이에서 이동/부스트/충돌이 안정적이고 재도전 가능     |
| **M2 2인 네트워크**  | 서로 다른 브라우저에서 동일 충돌 결과, 150ms RTT에서 부드러운 렌더 |
| **M3 Room 부하**     | 60명 봇 부하에서 20Hz 틱 예산 충족                                 |
| **M4 제품 흐름**     | 랜딩부터 결과까지 끊김 없는 E2E                                    |
| **M5 운영 준비**     | 설정 롤아웃/롤백, 로그/대시보드/알림                               |
| **M6 베타**          | 목표 입장 성공률, 크래시/오류, 재도전 지표 충족                    |

## 18.3 권장 역할

| **역할**                    | **책임**                                         |
|-----------------------------|--------------------------------------------------|
| **Tech/Product Lead**       | 기획 기준선, 아키텍처, 우선순위, 출시 승인       |
| **Game/Frontend Engineer**  | Phaser, 입력, 렌더, 예측/보간, React UI          |
| Backend/Realtime Engineer   | Room, collision, matchmaking, persistence, infra |
| **2D Artist/Animator**      | 브랜드, 스킨, atlas, UI asset, VFX               |
| **QA/Community (파트타임)** | 기기 테스트, 베타 운영, 신고/피드백              |

*1인 개발에서는 첫 세 역할을 한 명이 수행하되, 아트와 기기 QA는 외부 지원을 확보하는 편이 출시 품질과 일정에 유리하다.*

# 19. 리스크와 의사결정

| **ID**      | **등급** | **리스크**                             | **대응**                                               |
|-------------|----------|----------------------------------------|--------------------------------------------------------|
| **RISK-01** | High     | 긴 몸통/다수 펠릿에서 서버 틱 지연     | 공간 분할, 길이 압축, Room 정원 제한, 조기 부하 테스트 |
| **RISK-02** | High     | 지연으로 인한 “억울한 죽음”            | 보간, 명확한 판정, 지역 선택, 네트워크 품질 표시       |
| **RISK-03** | High     | 레퍼런스와 지나치게 유사한 브랜드/자산 | 독자 아트 디렉션, 상표 검색, 법률 검토                 |
| **RISK-04** | Medium   | 초기 사용자가 적어 빈 방 발생          | 봇 fill, 짧은 큐, 방 통합, 시간대별 capacity           |
| **RISK-05** | Medium   | Node 이벤트 루프/GC 변동               | 객체 풀링, worker/프로세스 격리, Rust 전환 경계 정의   |
| **RISK-06** | Medium   | 클라이언트 번들/모바일 GPU 과부하      | 지연 로드, DPR 제한, quality tier, atlas/batching      |
| **RISK-07** | Medium   | 랭킹/보상 중복                         | 멱등 결과 처리, DB unique constraint, outbox           |
| **RISK-08** | Low      | 운영 설정 오배포                       | schema validation, staged rollout, rollback, audit log |

## 19.1 주요 기술 의사결정 기록

| **ID**      | **결정**            | **이유**                                                                                 |
|-------------|---------------------|------------------------------------------------------------------------------------------|
| **ADR-001** | Phaser + React 분리 | 게임 루프와 제품 UI의 관심사를 분리하고 웹 2D 개발 생산성을 확보                         |
| **ADR-002** | 서버 권위형         | 치팅 방지와 모든 사용자에 대한 동일 판정                                                 |
| **ADR-003** | 20Hz 서버 틱        | 캐주얼 이동 게임의 반응성과 서버 비용 사이의 초기 균형                                   |
| **ADR-004** | Uniform Grid        | 동적 2D 객체가 많은 아레나에서 구현 복잡도와 성능의 균형                                 |
| **ADR-005** | Colyseus MVP        | Room/매치/상태 동기화를 빠르게 확보; 병목이 검증되면 game-core를 독립 서비스/Rust로 이동 |
| **ADR-006** | PostgreSQL + Redis  | 영속 트랜잭션과 단기 분산 상태의 역할 분리                                               |
| **ADR-007** | Room drain 배포     | 진행 중 실시간 경기를 강제 종료하지 않고 버전 전환                                       |

## 19.2 Go/No-Go 질문
- 60명 Room에서 최악 몸통 길이와 펠릿 수를 포함해 틱 P99가 예산 안에 들어오는가?

- 150~200ms RTT 환경에서 사용자가 조작이 불공정하다고 느끼지 않는가?

- 첫 방문자의 80% 이상이 설명 없이 이동·먹이·부스트를 이해하는가?

- 빈 방에서도 봇이 실제 사용자에게 과도하게 유리하거나 기만적으로 보이지 않는가?

- 브랜드, 이름, 그래픽, 사운드가 레퍼런스 서비스와 명확히 구분되는가?

- 오류/부하/배포 장애가 발생했을 때 담당자가 대시보드와 런북으로 대응할 수 있는가?

# 20. 출시 승인 기준

| **영역**   | **승인 조건**                                                         |
|------------|-----------------------------------------------------------------------|
| **제품**   | 첫 방문→입장 P95 10초 이내, 재도전 가능, 핵심 조작 튜토리얼 30초 이내 |
| **게임**   | 서버 권위 충돌/점수, 결정적 테스트, 주요 판정 이슈 해결               |
| **성능**   | 목표 동접 1.5배에서 틱/메모리/대역폭 기준 충족                        |
| **품질**   | 지원 브라우저/기기 테스트, 치명/높음 버그 0건                         |
| **운영**   | 대시보드, 알림, 설정 롤백, Room drain, 장애 런북                      |
| **보안**   | 토큰/입력/rate limit/admin MFA/비밀 관리 검증                         |
| **정책**   | 개인정보, 이용약관, 쿠키/분석, 신고·삭제 흐름                         |
| **브랜드** | 상표/도메인/스토어 이름 검토, 독자 자산 라이선스 기록                 |

## 20.1 MVP Definition of Done
- 새 사용자가 게스트로 생성되어 게임에 입장하고, 사망 후 결과가 저장되며 다시 플레이할 수 있다.

- 동일 Room의 모든 클라이언트가 같은 사망 원인과 순위를 수신한다.

- 서버 재배포 시 신규 Room은 신버전으로 생성되고 기존 Room은 정상 종료된다.

- 운영자가 활성 사용자, 방, 틱 지연, RTT, 오류율, 결과 backlog를 확인할 수 있다.

- 지원 브라우저에서 모바일/데스크톱 조작과 접근성 기본 옵션이 동작한다.

- 레퍼런스의 상표·자산·카피·화면을 사용하지 않고 자체 브랜드로 배포된다.

# 21. Loop Engineering 기반 구현 운영 가이드

이 장은 앞선 PRD를 AI 코딩 에이전트로 구현할 때 필요한 작업 분해, 반복 루프, 자동 검증, 역할 분리와 중단 규칙을 정의한다. 목적은 에이전트에게 “전체 게임을 한 번에 완성하라”고 지시하는 것이 아니라, 작고 검증 가능한 기능을 반복적으로 완성하는 개발 시스템을 만드는 것이다.

> **핵심 정의**
>
> Loop Engineering은 다음 작업 선택 → 코드 조사 → 최소 구현 → 자동 검증 → 수정 → 결과 기록 → 다음 작업으로 이어지는 제한된 반복 시스템이다. 무한 자동 코딩이 아니라 목표, 검증기, 상태 기록과 명확한 종료 조건을 가진 작업 운영 방식이다.

## 21.1 적용 원칙과 사람·에이전트의 역할

| **주체**               | **책임**                                            | **최종 권한**            |
|------------------------|-----------------------------------------------------|--------------------------|
| 제품 책임자/사람       | PRD, 우선순위, 게임 감각, 범위 변경, 배포 승인      | 제품·일정·병합·프로덕션  |
| Planner Agent          | PRD를 작업과 의존성으로 분해하고 실행 순서를 유지   | 작업 제안만 가능         |
| Implementer Agent      | 선택된 작업 하나의 코드와 테스트를 최소 범위로 구현 | 로컬 변경만 가능         |
| Reviewer Agent         | 아키텍처, 보안, 요구사항, 회귀 가능성 검토          | 승인 의견만 가능         |
| Test/Performance Agent | 테스트 누락, 틱·대역폭·메모리·AOI 성능 측정         | 검증 결과 판정           |
| Release Gate           | 모든 품질 기준을 종합해 병합 가능 여부를 판정       | 자동 배포·자동 병합 금지 |

- 한 번의 루프에서는 하나의 작업만 구현한다.

- 작업 완료는 에이전트의 설명이 아니라 테스트와 측정 결과로 판정한다.

- 게임 서버가 이동, 충돌, 점수, 길이와 사망 결과의 최종 권한을 가진다.

- 실시간 20Hz 틱 내부에서는 PostgreSQL, Redis 또는 외부 HTTP 호출을 하지 않는다.

- 에이전트는 프로덕션 배포, 실제 결제, 실사용자 데이터 접근, 비밀키 변경을 수행하지 않는다.

## 21.2 저장소와 명세 구조

DOCX PRD는 이해와 승인에 사용하고, 실제 에이전트 실행에 필요한 핵심 규칙은 저장소 내부의 Markdown·JSON·스크립트로 복제한다. 에이전트가 매 반복마다 전체 문서를 다시 해석하지 않도록 컨텍스트를 계층화한다.

**권장 저장소 구조**

```text
serpent-arena/
├─ AGENTS.md
├─ README.md
├─ docs/
│ ├─ prd.md
│ ├─ architecture.md
│ ├─ networking.md
│ ├─ game-rules.md
│ ├─ decisions.md
│ └─ known-issues.md
├─ specs/
│ ├─ milestones.yaml
│ ├─ tasks.json
│ ├─ acceptance-criteria.md
│ └─ performance-budget.md
├─ loops/
│ ├─ implement-next.md
│ ├─ review-current.md
│ ├─ networking-optimize.md
│ └─ load-test.md
├─ scripts/
│ ├─ verify.sh
│ ├─ run-local.sh
│ ├─ run-bots.sh
│ ├─ all-tasks-complete.sh
│ └─ check-task.ts
├─ apps/
│ ├─ web/
│ ├─ game-server/
│ └─ api/
└─ packages/
├─ game-core/
├─ protocol/
├─ config/
└─ test-utils/
```

## 21.3 구현 마일스톤과 종료 조건

| **단계**         | **구현 범위**                                                             | **핵심 종료 조건**                                        |
|------------------|---------------------------------------------------------------------------|-----------------------------------------------------------|
| M0 기반          | pnpm 모노레포, React/Phaser, 게임 서버, 공유 프로토콜, CI, Docker Compose | install·lint·typecheck·test·build 성공, 웹/서버 동시 실행 |
| M1 오프라인 코어 | 이동, 회전, 부스트, 먹이, 성장, 몸통, 경계, 충돌, 카메라                  | 10분 자동 플레이 안정, 동일 seed/input에서 동일 결과      |
| M2 권위형 2인    | Room, 입력 큐, 서버 이동, snapshot, 접속/퇴장, 사망/재시작                | 두 브라우저가 동일 서버 상태와 사망 원인을 확인           |
| M3 네트워크 감각 | sequence/ack, 예측, reconciliation, interpolation, jitter buffer          | RTT 100ms에서 즉시 조작감과 부드러운 원격 이동            |
| M4 대형 맵       | Spatial Hash, AOI, chunk 먹이, enter/leave, delta, 경로 압축              | AOI 외 엔터티 미전송, 틱·대역폭 예산 충족                 |
| M5 부하          | 봇 30/60/100명, 긴 몸통, 대량 먹이, 손실·지터·재접속 폭주                 | 목표 동접 1.5배에서 릴리즈 게이트 충족                    |
| M6 서비스        | 게스트/계정, 닉네임, 스킨, 전적, 리더보드, 운영자 도구                    | 권한·멱등성·데이터 보존·운영 플로우 검증                  |
| M7 배포          | 리전, 매치메이커, heartbeat, drain, 관측성, 오토스케일                    | 배포/롤백/드레이닝 리허설과 장애 런북 검증                |

> **순서 규칙**
>
> M1의 게임 코어를 네트워크와 분리해 먼저 완성하고, M2에서 단순한 전체 JSON snapshot으로 정확성을 확인한 뒤 M3~M4에서 예측·AOI·바이너리 최적화를 추가한다. 정확성과 관측성 없이 최적화부터 시작하지 않는다.

## 21.4 작업 단위와 상태 모델

좋은 작업은 하나의 명확한 동작을 바꾸고 자동 검증이 가능해야 한다. 한 작업당 변경 파일은 대체로 2~8개, 검증 명령은 1~5개 수준을 권장한다.

| **좋지 않은 작업**     | **권장 분해**                                                                     |
|------------------------|-----------------------------------------------------------------------------------|
| 멀티플레이어 전체 구현 | 입력 타입 → 입력 송신기 → 서버 입력 큐 → 이동 시뮬레이션 → snapshot → 원격 렌더링 |
| 네트워크 최적화        | sequence ack → reconciliation → interpolation buffer → delta → AOI → 양자화       |
| 서버 성능 개선         | 측정 지표 추가 → Spatial Hash → 객체 풀 → snapshot 예산 → 봇 판단 주기 조정       |

**tasks.json 작업 예시**

```json
{
  "tasks": [
    {
      "id": "NET-001",
      "milestone": "M2",
      "title": "플레이어 입력 메시지 구현",
      "status": "ready",
      "dependsOn": [],
      "scope": [
        "packages/protocol",
        "apps/web",
        "apps/game-server"
      ],
      "requirements": [
        "클라이언트는 sequence, angle, boosting만 전송한다",
        "서버는 클라이언트 좌표를 수신하지 않는다",
        "angle은 유효 범위로 정규화한다",
        "입력 빈도를 초당 30회 이하로 제한한다"
      ],
      "verification": [
        "pnpm test --filter protocol",
        "pnpm test --filter game-server",
        "pnpm typecheck"
      ],
      "done": false
    }
  ]
}
```

권장 작업 상태는 backlog, ready, in_progress, blocked, verification_failed, done이다. done은 모든 요구사항과 검증 명령이 성공한 경우에만 기록한다.

## 21.5 구현 루프 명세

| **요소**     | **정의**                                                                  |
|--------------|---------------------------------------------------------------------------|
| Trigger      | ready 상태이며 모든 선행 작업이 완료된 작업이 존재함                      |
| Goal         | 선택된 작업 하나의 요구사항과 검증 기준을 모두 충족                       |
| Context      | AGENTS.md, 관련 architecture/networking 문서, 작업 JSON, 최근 테스트 상태 |
| Actions      | 조사 → 계획 → 실패 테스트 → 최소 구현 → 검증 → 자체 리뷰 → 상태 기록      |
| Verification | 작업별 명령 + lint + typecheck + 관련 테스트 + 회귀 확인                  |
| Memory       | tasks.json, decisions.md, known-issues.md, 측정 결과 파일                 |
| Stop rule    | COMPLETE, BLOCKED, FAILED_VERIFICATION, OUT_OF_SCOPE 중 하나로 종료       |

에이전트가 같은 근본 원인으로 세 번 실패하면 무한 수정하지 않고 FAILED_VERIFICATION으로 종료한다. 외부 결정이나 접근 권한이 필요한 경우 임시 구현으로 덮지 않고 BLOCKED로 기록한다.

## 21.6 자동 검증과 성능 예산

루프의 품질은 프롬프트보다 검증기의 품질에 좌우된다. 완료 여부를 설명형 문장으로 판정하지 말고 컴파일, 단위 테스트, 통합 테스트, 브라우저 E2E와 성능 측정으로 단계화한다.

| **검증 레벨**  | **목적**                      | **예시**                                     |
|----------------|-------------------------------|----------------------------------------------|
| L1 정적 검증   | 문법·타입·규칙 오류 차단      | lint, typecheck, dependency boundary         |
| L2 단위 테스트 | 게임 규칙과 데이터 구조 검증  | 이동, 부스트, 충돌, Spatial Hash             |
| L3 통합 테스트 | 클라이언트·서버·프로토콜 연동 | Room 접속, input→snapshot, 재접속            |
| L4 E2E         | 실제 브라우저 플레이 플로우   | 게스트 시작, 두 브라우저 동기화, 사망/재시작 |
| L5 성능 게이트 | 실시간 게임 예산 유지         | tick P95/P99, 대역폭, 메모리, 렌더 FPS       |

| **항목**            | **초기 목표**    | **검증 조건**                                  |
|---------------------|------------------|------------------------------------------------|
| 서버 tick           | 20Hz / 50ms 간격 | 30명 P95\<5ms, 60명 P95\<10ms, 100명 P95\<20ms |
| 최대 tick           | \<40ms           | 부하 중 50ms 초과 지속 발생 없음               |
| Snapshot            | 10Hz             | P95 크기 4KB 이하, 기준 손실 시 resync         |
| 클라이언트 업로드   | 평균 5KB/s 이하  | 입력 빈도 제한과 바이너리 패킷 포함            |
| 클라이언트 다운로드 | 평균 30KB/s 이하 | 혼잡 P95 60KB/s 이하                           |
| 네트워크 환경       | RTT 30/100/200ms | loss 1/5%, jitter 30ms에서 플레이 가능         |
| 브라우저            | 기본 60FPS       | 저사양 모드는 30FPS, 지속 프레임 드롭 없음     |

**결정적 시뮬레이션 테스트 예시**

```typescript
it("produces the same state for the same seed and inputs", () => {
const first = runSimulation({ seed: 1234, inputs, ticks: 1_000 });
const second = runSimulation({ seed: 1234, inputs, ticks: 1_000 });
expect(second).toEqual(first);
});
```

- AOI 외부 플레이어는 snapshot에 포함하지 않는다.

- AOI 진입 시 entity-enter, 이탈 시 entity-leave 이벤트가 정확히 한 번 발생해야 한다.

- 경계에서 enter/leave가 반복되지 않도록 spawn/despawn 반경에 hysteresis를 둔다.

- 클라이언트 좌표, 점수, 길이와 먹이 획득 결과를 조작해도 서버 상태가 변하지 않아야 한다.

- 동일 seed와 입력 시 시뮬레이션 결과가 재현되어야 한다.

## 21.7 에이전트 역할 분리와 병렬 작업

탐색·구현·검증·리뷰를 한 에이전트의 긴 컨텍스트에 모두 넣으면 오류를 스스로 놓치기 쉽다. 역할별 에이전트 또는 독립 세션으로 결과를 요약해 전달한다.

| **역할**     | **주요 산출물**                         | **수정 권한**         |
|--------------|-----------------------------------------|-----------------------|
| Planner      | 작업 DAG, 선행 조건, 인수 조건          | specs/docs            |
| Implementer  | 코드, 단위/통합 테스트                  | 선택 task scope       |
| Reviewer     | 요구사항·아키텍처·보안 검토 보고서      | 기본 읽기 전용        |
| Performance  | 부하 시나리오, 벤치마크, 예산 초과 원인 | tests/scripts/metrics |
| Release Gate | 통과/차단 사유와 남은 위험              | 읽기 전용             |

| **병렬화하기 좋은 작업**      | **직렬화해야 하는 작업**      |
|-------------------------------|-------------------------------|
| 프런트 HUD와 서버 메트릭      | 공유 프로토콜과 네트워크 서버 |
| 스킨 UI와 부하 테스트 도구    | 게임 물리와 클라이언트 예측   |
| 문서와 독립적인 테스트 픽스처 | Player 상태 구조와 DB 모델    |
| 배포 설정과 그래픽 품질 옵션  | Room 생명주기와 매치메이커    |

**병렬 작업용 worktree 예시**

```bash
git worktree add ../serpent-net feature/networking
git worktree add ../serpent-ui feature/ui
git worktree add ../serpent-load feature/load-test
```

## 21.8 실행 방식과 점진적 자동화

처음에는 한 작업마다 사람이 diff와 검증 결과를 확인하는 반자동 방식으로 운영한다. 작업 템플릿과 테스트가 안정된 뒤에만 외부 반복 스크립트를 사용한다.

**Codex CLI 비대화형 실행 예시**

```bash
codex exec "
Read AGENTS.md and loops/implement-next.md.
Select and implement exactly one ready task from specs/tasks.json.
Run all required verification.
Do not commit or deploy.
Stop with COMPLETE, BLOCKED, FAILED_VERIFICATION, or OUT_OF_SCOPE.
"
```

**제한된 외부 반복 스크립트 예시**

```bash
#!/usr/bin/env bash
set -euo pipefail
MAX_ITERATIONS=5
for ((i=1; i<=MAX_ITERATIONS; i++)); do
echo "=== Loop iteration $i ==="
codex exec "$(cat loops/implement-next.md)"
if ./scripts/all-tasks-complete.sh; then
echo "All eligible tasks complete."
exit 0
fi
if ! ./scripts/verify.sh; then
echo "Verification failed. Stopping the outer loop."
exit 1
fi
done
exit 2
```

Codex CLI의 비대화형 모드는 스크립트와 CI에서 codex exec를 실행할 수 있다. Claude Code를 사용하는 경우 역할별 subagent로 컨텍스트를 분리하고, hook으로 포맷·검증·금지 명령을 결정적으로 실행하는 방식으로 동일한 운영 원칙을 적용할 수 있다. 구체적인 CLI 옵션은 도구 버전에 따라 변경될 수 있으므로 공식 문서를 기준으로 관리한다.

## 21.9 안전장치와 중단 규칙

| **안전장치** | **권장 정책**                                                            |
|--------------|--------------------------------------------------------------------------|
| 반복 예산    | 외부 루프 최대 3~5회, 작업당 동일 원인 실패 최대 3회                     |
| 변경 범위    | tasks.json의 scope 디렉터리 외 수정 금지, 파일 수 상한 설정              |
| Git          | 자동 force push·브랜치 삭제·자동 병합·프로덕션 태그 금지                 |
| 검증 우회    | 테스트 삭제, skip/only 추가, any/@ts-ignore 남용, lint 비활성화 금지     |
| 데이터       | 실사용자 데이터·실결제·실메일·실클라우드 자원 접근 금지                  |
| DB           | destructive migration 자동 실행 금지, expand/contract와 dry run 필수     |
| 비밀         | 실제 토큰·키 생성/저장 금지, 예제는 .env.example만 수정                  |
| 비용         | 봇 부하와 클라우드 테스트는 명시된 시간·인스턴스·예산 상한 내에서만 실행 |

| **종료 상태**       | **조건**                                   | **후속 처리**                   |
|---------------------|--------------------------------------------|---------------------------------|
| COMPLETE            | 모든 검증과 자체 리뷰 성공                 | done 기록 후 종료               |
| BLOCKED             | 외부 결정·접근 권한·누락된 선행 작업 필요  | known-issues에 질문과 영향 기록 |
| FAILED_VERIFICATION | 동일 근본 원인으로 3회 실패                | 로그·시도·남은 가설 기록        |
| OUT_OF_SCOPE        | 선택 작업과 현재 아키텍처·선행 상태 불일치 | 작업 재분해 요청                |

## 21.10 권장 첫 번째·두 번째 구현 루프

> **Loop 1 · 오프라인 코어**
>
> 브라우저에서 실행되는 오프라인 Serpent Arena를 완성한다. 마우스 방향 이동, 부스트, 먹이 획득, 길이 증가, 경로 기반 몸통, 충돌 사망, 카메라 추적을 포함한다. typecheck/build/unit test와 10분 자동 플레이가 성공해야 한다.

> **Loop 2 · 서버 권위형 2인 플레이**
>
> 두 브라우저가 같은 Room에 접속하고 서버가 계산한 위치를 공유한다. 클라이언트는 방향·부스트·sequence만 보내고 서버는 20Hz로 시뮬레이션하며 10Hz snapshot을 보낸다. 좌표 위조 무시, 접속 해제 제거, 동일 사망 판정이 검증되어야 한다.

> **Loop 3 · 지연 감추기**
>
> 클라이언트 예측, 서버 ack, reconciliation, 100ms interpolation buffer를 적용한다. RTT 100ms·jitter 30ms에서도 내 조작은 즉시 반응하고 상대 뱀은 눈에 띄는 순간이동 없이 움직여야 한다.

> **Loop 4 · 대형 맵 확장**
>
> Spatial Hash, AOI, chunk 먹이, enter/leave/delta와 몸통 경로 키포인트를 적용한다. 방 크기가 아니라 사용자별 AOI 엔터티 수에 비례하도록 CPU와 대역폭을 측정한다.

# 부록 A. 모노레포 권장 구조

```text
serpent-arena/
├─ apps/
│ ├─ web/ # React + Phaser PWA
│ ├─ api/ # REST API
│ ├─ matchmaker/ # match ticket / room selection
│ ├─ game-server/ # Colyseus rooms + authoritative simulation
│ ├─ worker/ # match result / leaderboard jobs
│ └─ admin/ # operations console
├─ packages/
│ ├─ game-core/ # deterministic movement/collision/scoring
│ ├─ protocol/ # shared schemas and message codecs
│ ├─ config/ # GameConfig schema/defaults
│ ├─ ui/ # shared React components
│ ├─ telemetry/ # logging/metrics helpers
│ └─ test-bots/ # load-test clients
├─ infra/
│ ├─ docker-compose/
│ ├─ terraform/
│ ├─ kubernetes/
│ └─ dashboards/
├─ assets/
│ ├─ source/
│ └─ manifests/
└─ docs/
├─ adr/
├─ runbooks/
└─ api/
```

# 부록 B. GameConfig 예시

```json
{
  "version": "2026-07-13.2",
  "mode": "classic",
  "arena": {
    "width": 20000,
    "height": 20000,
    "boundary": "lethal"
  },
  "room": {
    "maxPlayers": 60,
    "minHumans": 4,
    "maxBots": 20
  },
  "simulation": {
    "tickRate": 20,
    "snapshotRate": 10,
    "fixedDeltaMs": 50
  },
  "network": {
    "inputRate": 20,
    "interpolationDelayMs": 100,
    "maxExtrapolationMs": 200,
    "binaryProtocol": false,
    "targetBytesPerSecond": 40000
  },
  "interest": {
    "cellSize": 500,
    "radius": 1500,
    "despawnRadius": 1800,
    "targetNearbySnakes": 30
  },
  "snake": {
    "baseSpeed": 180,
    "boostSpeed": 270,
    "maxTurnRate": 3.2,
    "pathNodeMinDistance": 12,
    "pathNodeMinAngle": 0.08,
    "spawnProtectionMs": 1500
  },
  "pellets": {
    "targetCount": 12000,
    "chunkSync": true,
    "respawnBudgetPerTick": 30
  },
  "reconnect": {
    "graceMs": 10000
  },
  "leaderboard": {
    "size": 10,
    "updateHz": 4
  }
}
```

# 부록 C. 개발 착수 체크리스트

| **영역**       | **확인 항목**                                         |
|----------------|-------------------------------------------------------|
| **제품**       | 독자 브랜드/차별화 포인트/목표 플랫폼 확정            |
| **게임**       | arena/boundary/head-head/boost/respawn 정책 확정      |
| **프로토타입** | 오프라인 이동/몸통/충돌을 1주 내 검증                 |
| **네트워크**   | Room capacity와 tick/snapshot 초기값 확정             |
| **아키텍처**   | Colyseus 사용 범위와 game-core 경계 확정              |
| **자산**       | 기본 스킨 8~12종과 라이선스 계획                      |
| **인프라**     | 초기 region, 도메인, CDN, DB/Redis 제공자 선택        |
| **운영**       | 로그/메트릭/알림/런북 책임자 지정                     |
| **정책**       | 개인정보/쿠키/이용약관/신고 흐름 검토                 |
| **베타**       | 테스트 사용자, 목표 동접, 성공 지표, 피드백 채널 정의 |

# 부록 D. 참고 자료

| **ID** | **자료**                                                                            | **URL**                                                                                                                                                                           |
|--------|-------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **R1** | Snake.io 공식 웹사이트 — 웹 플레이, 플랫폼, 조작, 핵심 규칙, 실시간 멀티플레이 설명 | [https://snake.io/](https://snake.io/)                                                                                                                                     |
| **R2** | Apple App Store — 스킨, 미션, 리더보드, 월간 라이브 이벤트 설명                     | [https://apps.apple.com/kr/app/snake-io-fun-online-snake/id1104692136](https://apps.apple.com/kr/app/snake-io-fun-online-snake/id1104692136)                               |
| **R3** | Google Play — 부스트/조이스틱, 리더보드, 월간 이벤트, 보스 콘텐츠 설명              | [https://play.google.com/store/apps/details?id=com.amelosinteractive.snake](https://play.google.com/store/apps/details?id=com.amelosinteractive.snake)                     |
| **R4** | Phaser 공식 문서 — 웹 브라우저용 2D HTML5 프레임워크, WebGL/Canvas, TypeScript      | [https://docs.phaser.io/phaser/getting-started/what-is-phaser](https://docs.phaser.io/phaser/getting-started/what-is-phaser)                                               |
| **R5** | Colyseus 공식 문서 — 권위형 게임 서버, Room, 매치메이킹, 상태 동기화                | [https://docs.colyseus.io/](https://docs.colyseus.io/)                                                                                                                     |
| **R6** | Kubernetes 공식 문서 — Horizontal Pod Autoscaler                                    | [https://kubernetes.io/docs/concepts/workloads/autoscaling/horizontal-pod-autoscale/](https://kubernetes.io/docs/concepts/workloads/autoscaling/horizontal-pod-autoscale/) |
| R7     | OpenAI Codex CLI — 로컬 코드 읽기·수정·명령 실행                                    | <https://developers.openai.com/codex/cli> |
| R8     | OpenAI Codex 비대화형 모드 — codex exec를 스크립트와 CI에서 사용                    | <https://developers.openai.com/codex/noninteractive> |
| R9     | OpenAI Codex Subagents — 전문 에이전트 병렬 실행                                    | <https://developers.openai.com/codex/subagents> |
| R10    | Anthropic Claude Code Subagents — 역할별 독립 컨텍스트                              | <https://docs.anthropic.com/en/docs/claude-code/sub-agents> |
| R11    | Anthropic Claude Code Hooks — 생명주기별 결정적 명령 실행                           | <https://docs.anthropic.com/en/docs/claude-code/hooks-guide> |

> **네트워크 설계 출처 범위**
>
> 9.8장의 수치와 패킷 구조는 공개된 Snake.io 내부 구현값이 아니라 일반적인 서버 권위형 실시간 게임 기법과 본 제품의 성능 목표를 바탕으로 한 설계 기준이다.

# 부록 E. AGENTS.md 기본 템플릿

```markdown
# Project Rules
## Stack
- Package manager: pnpm
- Language: TypeScript strict mode
- Client: React + Phaser
- Game server: Node.js + Colyseus
- Shared simulation: packages/game-core
- Shared protocol: packages/protocol
## Architecture
- The server is authoritative.
- Never trust client coordinates, score, length, food pickup, or collision results.
- Do not access PostgreSQL, Redis, or external HTTP inside the 20 Hz simulation loop.
- Keep Phaser rendering code outside game-core.
- Avoid duplicate protocol definitions between client and server.
## Work Rules
- Complete exactly one task per iteration.
- Stay inside the task scope unless a documented dependency requires otherwise.
- Add or update tests for every behavioral change.
- Do not perform unrelated refactoring.
- Record important decisions in docs/decisions.md.
## Verification
- Run task-specific verification.
- Run lint, typecheck, related tests, and build.
- Never weaken tests to pass verification.
- Do not hide errors with any, @ts-ignore, skipped tests, or disabled rules.
## Safety
- Do not deploy or change production resources.
- Do not access real users, payments, email, or secrets.
- Do not run destructive database migrations.
- Do not force push, delete branches, or auto-merge.
- Stop after three failures with the same root cause.
```

# 부록 F. implement-next.md 루프 템플릿

```markdown
# Implement Next Task Loop
## Goal
Select exactly one eligible task from specs/tasks.json, implement it, and satisfy every verification criterion.
## Required Context
1. AGENTS.md
2. docs/architecture.md
3. Relevant docs/*.md
4. specs/tasks.json
5. Current test and build status
## Task Selection
Choose the first task where:
- status is ready
- done is false
- every dependsOn task is complete
- no other agent owns the task
Do not implement more than one task in a single iteration.
## Workflow
1. Summarize requirements and acceptance criteria.
2. Inspect relevant code and tests.
3. Write a minimal change plan.
4. Add a failing test or demonstrate the existing failure.
5. Make the smallest necessary implementation.
6. Run task-specific verification.
7. Run lint, typecheck, related tests, and build.
8. Review the diff for scope, architecture, security, and regressions.
9. Mark done only after all checks pass.
10. Record important decisions or blockers.
## Terminal States
- COMPLETE: all checks pass
- BLOCKED: external decision, access, or missing dependency
- FAILED_VERIFICATION: same root cause failed three times
- OUT_OF_SCOPE: task conflicts with architecture or prerequisites
## Prohibited Actions
- Production deployment or real cloud mutations
- Real credentials, user data, payments, or email
- Destructive migrations
- Force push, branch deletion, automatic merge
- Test deletion, skip/only, any, @ts-ignore, or rule disabling to hide failures
```

# 부록 G. 검증 스크립트 템플릿

**scripts/verify.sh**

```bash
#!/usr/bin/env bash
set -euo pipefail
pnpm lint
pnpm typecheck
pnpm test
pnpm test:integration
pnpm build
```

**scripts/verify-game-server.sh**

```bash
#!/usr/bin/env bash
set -euo pipefail
pnpm --filter game-core test
pnpm --filter protocol test
pnpm --filter game-server test
pnpm test:simulation-determinism
pnpm test:network-emulation
pnpm test:tick-budget
```

**scripts/run-bots.sh**

```bash
#!/usr/bin/env bash
set -euo pipefail
CONCURRENCY="${1:-30}"
DURATION="${2:-300}"
pnpm --filter load-test start -- \
--players "$CONCURRENCY" \
--duration "$DURATION" \
--scenario classic-aoi
```

# 부록 H. Loop Engineering 착수 체크리스트

| **영역** | **착수 조건**                                                      |
|----------|--------------------------------------------------------------------|
| 문서     | PRD 핵심 내용을 docs/prd.md, architecture.md, networking.md로 변환 |
| 규칙     | AGENTS.md에 권위형 서버, 작업 범위, 안전 금지 사항 기록            |
| 작업     | M0~M2를 1동작 단위 tasks.json으로 분해하고 dependsOn 설정          |
| 테스트   | 결정성, 입력 검증, 충돌, Room 접속의 최소 테스트 기반 확보         |
| 스크립트 | verify.sh, run-local.sh, run-bots.sh를 사람이 직접 실행해 검증     |
| 상태     | ready/in_progress/blocked/failed/done 상태 전이 규칙 확정          |
| 권한     | 에이전트가 수정 가능한 디렉터리와 실행 가능한 명령 범위 제한       |
| 병합     | 자동 병합 없이 사람이 diff·검증 결과·성능 변화 확인                |
| 자동화   | 10개 이상 작업이 안정적으로 완료된 뒤 외부 반복 루프 활성화        |

> **도구 버전 주의**
>
> Codex CLI, Claude Code와 기타 AI 코딩 도구의 명령·권한 모델은 변경될 수 있다. 이 문서의 핵심은 특정 제품 기능이 아니라 작업 단위, 검증기, 상태 기록, 안전장치와 중단 규칙이며, 실제 실행 옵션은 도입 시점의 공식 문서로 확인한다.

---

**END OF DOCUMENT**
